# Vendor reference docs — large PDFs/HTML/assets, not version-controlled.
# See docs/reference/README.md for what belongs here and where to download each.
docs/reference/*
!docs/reference/README.md

# Devin local config — personal MCP server paths, API keys, etc.
.devin/config.local.json

# Zephyr build output — generated by `west build`, not version-controlled.
firmware/build/
