# Project Log

## User Background

- Senior year Computer and Systems Engineering student.
- Experience level: between "some hardware experience" and "moderate hardware" — has worked with microcontrollers and designed a few simple PCBs.
- Wants this project to push beyond prior experience into RF, cellular comms, power management, and multi-discipline integration.
- Open to simplifying initial versions but wants the overall project to be challenging and time-consuming.
- Has access to FDM, SLA, and CNC for enclosure fabrication.

## Decision Log

### 2026-06-28: Problem Definition Session
- **Decision**: Target US market — LTE with VoLTE is the only viable network path.
  - *Rationale*: 2G and 3G are shut down in the US. No point building for dead networks.
- **Decision**: Carrier-agnostic, but recommend T-Mobile/Mint for testing.
  - *Rationale*: T-Mobile is most lenient with non-certified devices on prepaid. Good LTE band 2/4/66 coverage.
- **Decision**: MVP = voice calls only (make/receive). Contacts, SMS, menus are post-MVP.
  - *Rationale*: Get to a working call as fast as possible to validate the hardest part (cellular + audio + firmware). Everything else is additive.
- **Decision**: Use RTOS (FreeRTOS or Zephyr) for firmware, not bare metal.
  - *Rationale*: Concurrent requirements (AT command parsing, UI, power management, call state machine) make bare metal unwieldy for a daily-driver. RTOS provides task scheduling without sacrificing control. FreeRTOS vs Zephyr TBD.
- **Decision**: Form factor/mechanical design deferred. Start with single-board design.
  - *Rationale*: The hard problems (cellular, firmware, power, RF) are form-factor independent. Locking into flip early adds constraints (flex cable, hinge, multi-board) before core electronics are validated. Decide mechanical design after the phone works on a single board.
- **Decision**: ~~Display type deferred — will recommend based on constraints.~~
  - **SUPERSEDED 2026-06-28**: Display selected — ST7789V SPI color TFT (2.0" 240×320, RGB565). See "Display Selection" decision below.
- **Decision**: ~~Keypad design deferred to Phase 2 prototyping.~~
  - **SUPERSEDED 2026-06-28**: Keypad selected — SMD tactile switches on custom PCB traces (LOCKED). Phase 2 prototyping uses off-the-shelf 4×4 matrix module + loose tactile buttons. See "Keypad Selection" decision below.

### 2026-06-28: Modular Ecosystem Vision
- **Decision**: Adopt a long-term vision of a personal ecosystem of targeted devices, with the phone as the connectivity hub.
  - *Rationale*: Building one perfect do-everything phone is unrealistic as a first project. Decomposing into targeted modules (car system, etc.) makes each piece achievable. The phone remains the primary project.
- **Decision**: First envisioned module is a car system (navigation + music), connecting to phone via USB.
  - *Rationale*: Car dock scenario makes USB ideal — provides data + power in one cable, higher bandwidth than Bluetooth, lower latency. Phone charges while docked. Android USB tethering is native. Car system offloads the hardest work (cellular) to the phone.
- **Decision**: USB is the primary ecosystem interconnect; Bluetooth/WiFi deferred for future wireless modules.
  - *Rationale*: USB is simpler, higher bandwidth, and provides power. Wireless is needed only for modules that can't be physically docked — that's a future problem.
- **Decision**: Ecosystem vision does not change MVP scope, but does constrain hardware selection — MCU must have USB capability.
  - *Rationale*: Selecting an MCU without USB would preclude future ecosystem integration. This is a low-cost constraint (many candidate MCUs have USB) that keeps the door open.

### 2026-06-28: Cellular Module Architecture & Selection
- **Decision**: Use Architecture A — LTE Cat 4 data module + external MCU + external audio codec (not OpenCPU, not analog-audio voice module, not premium carrier-certified module).
  - *Rationale*: Four architectures were evaluated:
    - A: Cat 4 module + MCU + codec (SIM7600/EG25-G class)
    - B: Cat 1 voice module with analog audio pins (Quectel EG912U-GL) — eliminates codec for voice, but music playback needs a separate DAC, creating two audio paths instead of one
    - C: OpenCPU/QuecPython smart module (module IS the MCU, Python firmware) — eliminates MCU/RTOS learning, which is a core project goal
    - D: Premium carrier-certified module (u-blox LARA-R6401, now Trasna) — best VoLTE/carrier support but **disqualified: 911/E911 not supported** (updated 2026-06-28 second round; originally eliminated for supply chain risk, but the 911 issue is the real disqualifier)
  - Architecture A chosen because: (1) music player goal requires a real audio DAC regardless of module, so the codec isn't redundant — it handles both voice (PCM from module) and music (I2S from MCU); (2) preserves full MCU + RTOS + embedded firmware learning; (3) lowest supply risk; (4) Cat 4 gives headroom for ecosystem tethering. **Note (2026-06-28)**: The original claim that a single codec "unifies" PCM and I2S "into one audio path" was found to be false for common single-port codecs (WM8960, NAU8810, etc.) — they have only one digital audio port. The MAX9880A (dual-port codec) is what makes Architecture A actually work. See Codec Selection decision below.
- **Decision**: Select SIMCom SIM7600 series as the cellular module (not Quectel EG25-G).
  - *Rationale*: SIM7600A-H (NA variant) has B66 and B71 (T-Mobile 600MHz Extended Range LTE); EG25-G lacks both. SIM7600A-H has explicit T-Mobile carrier certification; EG25-G does not. Forum evidence shows EG25-G VoLTE falls back to CS (which fails on T-Mobile since 3G shutdown) with carrier-provisioning dependencies that hobbyists can't resolve; SIM7600 has a documented, workable PDP context quirk instead. Waveshare SIM7600G-H 4G HAT provides a complete prototyping platform (NAU8810 codec, SIM slot, antennas, USB-to-UART) ready for immediate VoLTE validation — no equivalent EG25-G breakout with audio.
  - *Tradeoff*: SIM7600's IMS PDP context (cid=2 for VoLTE) can conflict with data on cid=1, requiring careful firmware management of multiple PDP contexts. This is relevant for the ecosystem goal of simultaneous VoLTE + data tethering. Manageable but a real firmware complexity item.
- **Decision**: ~~Prototyping path — Waveshare SIM7600G-H 4G HAT + Mint Mobile SIM for initial VoLTE/audio validation on a PC via USB-to-UART, before any MCU or PCB work.~~
  - **SUPERSEDED 2026-06-28**: Switched to Waveshare SIM7600NA-H 4G HAT (~$89) which has B71 + NAU8810 codec. See "Modem Revisit" decision below. The G-H HAT lacks B71 (T-Mobile 600 MHz), creating a validation gap. The NA-H HAT resolves this.
  - *Rationale*: De-risks the hardest part (VoLTE on a real US carrier) this week with zero custom hardware. HAT includes the NAU8810 codec so voice audio can be tested immediately.
- **Decision**: ~~Final PCB will use SIM7600A-H (NA variant with B66/B71) + a codec capable of both PCM (voice from module) and I2S (music from MCU), e.g., WM8960 or NAU8810-class.~~
  - *Rationale*: ~~Single codec handles both voice and music through one audio path. A-H variant gets full T-Mobile band coverage for the daily-driver goal.~~
  - **SUPERSEDED 2026-06-28**: The codec choice (WM8960 or NAU8810-class) is superseded by the MAX9880A selection. WM8960 and NAU8810 are single-port codecs that cannot accept PCM and I2S simultaneously — the "single codec handles both" claim was false for these parts. The MAX9880A (dual-port codec) is the selected replacement. The SIM7600A-H module choice is retained. See "Codec Selection / Audio Path Architecture" decision below.

### 2026-06-28: RTOS Selection
- **Decision**: Use Zephyr RTOS (not FreeRTOS).
  - *Rationale*: Every subsystem the phone needs is built into Zephyr: USB device stack (CDC ACM, ~~CDC ECM for tethering~~ [superseded — see USB HS/ULPI Revisit: tethering now uses the SIM7600's own USB port, not MCU CDC ECM], MSC for storage access), PPP networking over cellular modem, FatFs/LittleFS for SD card, LVGL for display, power management subsystem for battery policy, BLE stack. With FreeRTOS these would be 5+ separate third-party integrations (TinyUSB, lwIP, FatFs, LVGL, vendor BT stack) that don't always compose cleanly.
  - *Learning alignment*: User explicitly wants a challenging, time-consuming project that pushes beyond prior experience. Zephyr's devicetree + Kconfig + west + subsystems model teaches modern embedded industry practices (Linux-grade workflows) that transfer to Nordic, NXP, Intel, TI ecosystems. The steep learning curve is a feature, not a bug, for this project's goals.
  - *Ecosystem advantage*: ~~CDC ECM (Ethernet-over-USB) is a built-in Zephyr USB function — the phone-as-USB-Ethernet-device for the car module tethering scenario is a config flag, not a from-scratch USB class implementation.~~ **SUPERSEDED 2026-06-28**: The phone-as-USB-Ethernet-device (MCU bridging LTE to CDC ECM) is no longer the tethering architecture — the SIM7600's own USB port does RNDIS/ECM directly. The Zephyr CDC ECM function is no longer needed for tethering (though it remains available if a future use case wants MCU-originated Ethernet-over-USB). See "USB HS / ULPI Revisit" decision below.
  - *Nordic native path*: nRF Connect SDK is Zephyr-based. If nRF52840 is selected for MCU, Zephyr is the native RTOS.
  - *Tradeoff*: Zephyr has a steep learning curve (devicetree, Kconfig, west build system). Expect 1-2 weeks of ramp-up before productive phone firmware work. Mitigation: complete the "Practical Zephyr" tutorial series (memfault) and Shawn Hymel's Zephyr guide before starting phone firmware.
  - *Cellular modem note*: Neither Zephyr nor FreeRTOS has ready-made SIM7600 support. Zephyr's modem subsystem (modem_chat + CMUX + PPP + pipelink) provides a real architecture to plug a SIM7600 port into; FreeRTOS Cellular Library would require building that infrastructure from scratch. This is a wash on effort but Zephyr provides better scaffolding.
- **Decision**: Adopt a "no feature blocking" principle for component selection — all features rated 5+ on the wishlist must remain achievable (hardware-capable, even if not implemented in MVP).
  - *Rationale*: Avoiding rework and respins. If a component choice blocks a 5+ feature, we'd need to swap the component later (MCU change = PCB respin + firmware rewrite). Selecting components with headroom for 5+ features costs little upfront and preserves all options.
  - *5+ features that constrain MCU selection*: USB device (8), SPI/SDIO for MicroSD (7), BLE (6), I2S/audio DAC (6), camera interface (6), color display interface (5-6), video recording processing headroom (5).

### 2026-06-28: Modem Revisit (SIM7600 Selection Review)
- **Decision**: **Stick with SIMCom SIM7600 series** — the four concerns raised in the modem revisit prompt do not collectively justify switching. Two changes adopted.
  - *Rationale*: Four concerns were researched against SIM7600, EG25-G, EG912U-GL, and LARA-R6401:
    1. **Standby power**: The docs' "3 mA sleep" figure was deep-sleep (cannot receive calls), not idle/DRX. Actual SIM7600 LTE idle/DRX current is **17.5 mA** (datasheet: SIM7600E Hardware Design V1.00 Table 26). 24h standby needs ~420 mAh — achievable with an 800+ mAh battery. The modem still dominates standby; MCU sleep (~20 µA) is negligible. EG25-G is worse (22 mA); EG912U-GL is better (14 mA) but eliminated for the two-audio-path problem; LARA-R6401 claims ~1.1 mA (unverified). This concern does not justify switching.
    2. **B71 validation gap (RESOLVED)**: Waveshare makes a **SIM7600NA-H 4G HAT** (~$89) with B71 and the same NAU8810 codec. Switching the prototyping HAT from G-H to NA-H closes the gap — B71 can be validated early without a custom PCB. B71 is supplementary in the user's area (good coverage) but cheap to validate.
    3. **Simultaneous VoLTE + data (real risk, future feature)**: Forum evidence (TinyGSM #649, LilyGO #90) shows SIM7600 PDP context conflicts (IMS cid=2 interferes with data cid=1). No clear hobbyist evidence of successful simultaneous VoLTE+data on SIM7600. EG25-G has better-documented multi-PDP support (PinePhone-proven); LARA-R6401 has best multi-PDN docs. **However**: this is a future ecosystem feature (tethering while on a call), NOT an MVP or daily-driver requirement. The MVP (make/receive calls) is unaffected. Make this the first HAT test; if it fails, it blocks a future feature, not the core phone. Fallback: EG25-G (loses B71) or LARA-R6401 (supply risk).
    4. **LGA assembly (wash)**: All candidate LTE modules are LGA — none are hand-solderable. No LTE/VoLTE module exists in a hand-friendly package. NFR-3 ("hand-solderable for prototypes") is unachievable for any LTE module. Realistic path: JLCPCB assembly for the modem section (~$57–72), hand-solder the rest.
- **Change 1**: Switch prototyping HAT from SIM7600G-H to **SIM7600NA-H** (~$89) — resolves B71 validation gap. Has B71 + NAU8810 codec + GNSS.
- **Change 2**: Make **simultaneous VoLTE + data the first HAT test** (AT command sequence in research-notes.md modem revisit findings). If it fails after genuine effort, revisit with EG25-G or LARA-R6401 as fallbacks — but only for the ecosystem tethering feature, not the MVP.
- **Carrier note**: T-Mobile/Mint is a recommendation, not a lock-in. T-Mobile is most lenient with non-certified devices on prepaid. AT&T/Verizon are stricter on IMEI whitelisting. SIM7600A-H supports bands for all three major US carriers — switching later is possible.
- **Tradeoff**: The SIM7600's PDP context management is a real firmware complexity item and simultaneous VoLTE+data is unproven. The EG25-G would be safer for the ecosystem feature but loses B71 (the original reason for choosing SIM7600). The decision is to validate on the HAT first rather than switch speculatively.
- **SUPERSEDED 2026-06-28 (same day, second round)**: The framing of simultaneous VoLTE+data as "the first HAT test" and as a real risk was overstated. The second-round re-evaluation (below) determined that (a) the PDP context conflict is specific to the `AT+NETOPEN` code path, not the CMUX+PPP path the project uses; (b) simultaneous VoLTE+data is not a requirement for any current phase; and (c) LARA-R6401 is disqualified entirely (911 not supported). See "Modem Revisit (Second Round)" decision below. The two changes (NA-H HAT, VoLTE+data testing) are retained but de-prioritized — VoLTE+data is now an informational HAT test, not a blocking one.

### 2026-06-28: Modem Revisit (Second Round — Full Re-evaluation)
- **Decision**: **SIM7600 confirmed and locked.** No alternative module justifies switching. The modem selection is resolved.
  - *Rationale*: A deeper re-evaluation examined LARA-R6401 supply chain, EC25-AF as a new candidate, the EG25-G T-Mobile VoLTE issue, and the true scope of the SIM7600 PDP context conflict.
    1. **LARA-R6401 — DISQUALIFIED (911/E911 not supported)**: The LARA-R6401 datasheet explicitly states "The 911 and E911 services are not supported." A phone that cannot call 911 is a safety issue. This is a hard disqualifier regardless of its strengths (best multi-PDN, I2S audio, ~1.1 mA idle, carrier certs). Supply risk (Trasna transition) was the initial concern but is secondary — the module is in mass production and available at DigiKey. No built-in GNSS. Small community.
    2. **EC25-AF — strong alternative, not worth switching**: T-Mobile voice certified (unlike EG25-G which is data-only), B71, optional GNSS, 911/eCall supported, multi-PDP with IMS auto-isolated. Available at DigiKey (Mini PCIe ~$60). However: no breakout with integrated codec (slower prototyping), higher idle current (23.3 mA vs 17.5 mA), and the multi-PDP advantage is irrelevant (simultaneous VoLTE+data not required). **Documented as fallback for final PCB if SIM7600 testing reveals a blocking issue.**
    3. **EG25-G T-Mobile VoLTE — clarified**: The EG25-G's VoLTE CS fallback on T-Mobile is a carrier provisioning issue (T-Mobile certifies EG25-G for data only, not voice), not a hardware limitation. The EC25-AF is the proper Quectel alternative (T-Mobile voice certified).
    4. **SIM7600 PDP context conflict — RESOLVED (not a blocker)**: The conflict is confirmed in the `AT+NETOPEN` code path only. This project uses CMUX+PPP (`ATD*99***<cid>#`), which bypasses `AT+NETOPEN` entirely. CMUX+PPP is the standard documented pattern for simultaneous data + AT commands on the SIM7600 (Linux n_gsm, RT-Thread, ESP-IDF). Whether data stays active *during* a live VoLTE call via PPP is unverified but likely (3GPP bearers should coexist). Fallback: pause data during calls — acceptable.
    5. **Simultaneous VoLTE+data — NOT a requirement**: MVP is make/receive calls. Daily driver adds SMS/contacts/menu. None need data during a call. The future ecosystem (car tethering while on a call) is the only scenario, and "pause data during call" is acceptable there too. This was incorrectly treated as a near-term constraint in the first revisit; corrected.
  - *Why SIM7600 wins*: Waveshare NA-H HAT with codec (fast prototyping), built-in GNSS, large community, lower idle current (17.5 mA vs EC25-AF's 23.3 mA), 911 supported. The one advantage EC25-AF had (better multi-PDP) addresses a need the project doesn't have.
  - *Fallback*: EC25-AF for the final PCB if SIM7600 HAT testing reveals a blocking issue. Both are Cat 4, T-Mobile voice certified, B71-capable — switching at PCB time is low-cost.
  - *Tradeoff*: The SIM7600's `AT+NETOPEN` bug is real but irrelevant to this project's CMUX+PPP architecture. Data-during-VoLTE-call is unverified via PPP; if it fails, pause data during calls. No modem switch needed.

### 2026-06-28: MCU Selection
- **Decision**: Select STM32H743 (Cortex-M7 @ 480MHz) as the MCU, in LQFP-144 package (STM32H743ZI).
  - *Rationale*: Four MCU candidates were evaluated against the "no 5+ features blocked" principle and the concurrent processing load requirement:
    - **nRF52840**: Eliminated — no camera interface (blocks photo capture 6, video 5), I2S clock accuracy problems, USB device only (no OTG), 64MHz is tight for MP3 decode.
    - **ESP32-S3**: Viable but with Zephyr USB support only 8 months old (risk for ecosystem tethering), and BLE advantage is moot since neither ESP32-S3 nor STM32 has classic BT (A2DP) — external BT module needed either way.
    - **ESP32-P4**: Most capable for multimedia (hardware H.264, MIPI camera/display) but too new — limited availability, immature Zephyr support, no built-in wireless, likely BGA-only. Too risky for first custom PCB.
    - **STM32L4R5**: Questionable on processing power — 120MHz cacheless M4 running Zephyr's full networking + USB + LVGL + audio stack concurrently is tight, especially for ecosystem tethering (PPP routing + USB CDC ECM + audio + display).
    - **STM32H743**: 480MHz Cortex-M7 with L1 cache (16KB I + 16KB D), hardware JPEG codec, USB OTG FS + HS, DCMI camera, 4x SAI audio, 2x SDMMC, LTDC display controller. Handles the worst-case concurrent load (tethering + music + UI + call) comfortably. ARM Cortex-M7 (career-relevant). Mature Zephyr support (multiple H7 boards in mainline). ~$12-15, within budget.
  - *Processing power analysis*: The real CPU demand isn't any single feature — it's the concurrent subsystem load. Worst case: ~~ecosystem tethering (USB CDC ECM + PPP routing + potentially NAT)~~ [superseded 2026-06-28 — tethering now runs on the modem's USB, not the MCU; MCU load for tethering is ~zero], MP3 decode, LVGL rendering, and modem AT command management all running simultaneously. This is essentially a media player + UI + phone (the router role moved off the MCU). The H7's 480MHz + L1 cache is still the right class of part for this; cacheless M4 parts (L4) would struggle with the remaining media + UI + modem load.
  - *Package choice (LQFP-144)*: The H743 comes in LQFP-100, -144, and -176 (all 0.5mm pitch). LQFP-100 loses USB HS (ULPI requires 12 dedicated pins that don't fit). LQFP-144 retains USB HS and all needed peripherals. LQFP-176 adds little for this project. LQFP-144 is hand-solderable with practice; JLCPCB assembly is the fallback (and will be needed for the SIM7600 LGA module regardless).
  - *Tradeoffs*: Higher power consumption (~20 µA sleep vs 0.4 µA for L4) — but modem dominates at 17,500 µA (17.5 mA LTE idle/DRX, verified 2026-06-28), so MCU sleep current is negligible. Larger package than L4. No built-in BLE — external BT module needed (same as any STM32 path, and same as ESP32-S3 for classic BT).
  - *USB HS note*: ~~True USB High-Speed (480 Mbps) requires an external ULPI transceiver (e.g., USB3300, ~$2). Can start with USB FS (built-in PHY) for MVP and populate ULPI transceiver later for ecosystem tethering. LQFP-144 keeps this option open.~~ **SUPERSEDED 2026-06-28**: USB3300 ULPI dropped — the SIM7600's own USB 2.0 HS port does tethering directly, bypassing the MCU. MCU USB OTG_FS is sufficient. LQFP-144 retained for GPIO margin. See "USB HS / ULPI Revisit" decision below.
- **Decision**: Bluetooth will be added as an external module (not built into MCU).
  - *Rationale*: Neither STM32H7 nor ESP32-S3 has classic Bluetooth (A2DP for audio streaming). Both have BLE (ESP32-S3 built-in, STM32 via external module). Since an external BT module is needed for classic BT regardless of MCU choice, the STM32's lack of built-in BLE is not a disadvantage. An external BT module (e.g., BM83 for classic BT + BLE) can be added when the Bluetooth feature (rated 6) is implemented. This defers the BT component decision without blocking the feature.

### 2026-06-28: Codec Selection / Audio Path Architecture
- **Decision**: Select **MAX9880A** (Maxim/ADI, TQFN-48, ~$1.70) as the audio codec. Dual-port architecture: SIM7600 PCM → MAX9880A primary port (voice), STM32H743 I2S → MAX9880A secondary port (music). **MCU is NOT in the voice audio path during calls.** LOCKED 2026-06-28.
  - *Rationale*: The revisit was triggered by a HIGH PRIORITY open question: the docs claimed a single codec (e.g., WM8960) "unifies" voice (PCM from module) and music (I2S from MCU) into one audio path, but most codecs appeared to be I2S-only with no second PCM port. Three research threads confirmed:
    1. **SIM7600 is PCM-only** (Hardware Design Manual V1.03 §3.6): fixed master mode, short-frame sync, 16-bit linear, 2048/4096kHz clock. No I2S mode, not configurable via AT commands. The Waveshare HAT's NAU8810 is wired in PCM mode to match.
    2. **All common codecs have ONE digital audio port** — WM8960, NAU8810, NAU8822, SGTL5000, TLV320AIC3204/3104, ES8316/8388. They can be configured for I2S *or* PCM, but not both simultaneously. The original "single codec unifies voice and music" claim was **false** for all these parts. The TLV320AIC3106's "alternate bus" is pin multiplexing, not a second independent port (confirmed by TI forum).
    3. **MAX9880A is the only hobbyist-accessible dual-port codec** — two truly independent digital audio interfaces (primary for voiceband PCM/TDM, secondary for stereo I2S/TDM), running simultaneously and asynchronously. ~$1.70 in TQFN-48 (hand-solderable with hot air). Designed specifically for smartphones (voice + media). The TLV320AIC34 is also dual-port but BGA-only (not hobbyist-friendly).
  - *Why MAX9880A over MCU bridge*: The alternative was an MCU bridge (SAI1 PCM slave from SIM7600 → SAI2 I2S master to single-port codec). This is feasible on the H743 (~8-10ms latency, <1% CPU, well within ITU G.114) but has two problems: (a) the MCU is always in the voice audio path, adding latency and firmware complexity (real-time DMA bridging, cache coherency, codec rate switching between 8kHz calls and 48kHz music); (b) Zephyr's STM32H7 SAI driver is immature (PR #92887 in progress, not confirmed merged) — could require driver porting or direct HAL usage. The MAX9880A avoids both issues: voice goes directly module→codec (MCU not involved), and the MCU I2S for music is simpler and more tolerant. The MAX9880A is also cheaper ($1.70 vs WM8960 $6-12 obsolete) and matches real smartphone architecture.
  - *Why not two codecs* (NAU8810 for voice PCM + PCM5102 for music I2S): This is the "two audio paths" problem originally rejected — needs an analog mixer/switch to combine outputs to the speaker, more components, more PCB complexity. The MAX9880A does the mixing internally. Eliminated.
  - *Pre-PCB verification items*: (1) Confirm MAX9880A primary port supports PCM short-frame sync, 16-bit, master clock from module (check datasheet — subagent reported "TDM and I2S formats"; PCM short-frame is a common TDM subset but must be confirmed). (2) Verify stock availability at DigiKey/Mouser (Maxim/ADI parts can have long lead times). (3) MAX9880A is 1.8V supply — add 1.8V LDO and level shifter for MCU's 3.3V I2S lines (unidirectional, simple). (4) MAX9880A is NOT on the Waveshare HAT (HAT has NAU8810) — committed for final PCB, not prototyped on HAT.
  - *Fallback*: If MAX9880A is unavailable or PCM-incompatible, fall back to MCU bridge architecture with NAU8822 (single-port I2S codec, ~$1.20). SAI1 as PCM slave, SAI2 as I2S master. Run codec at 8/16kHz during calls, 48kHz for music. Requires Zephyr H7 SAI driver. See research-notes.md Codec Selection Finding 3.
  - *Prototyping impact*: The Waveshare NA-H HAT prototyping step is **unchanged** — it validates the SIM7600's PCM voice output on T-Mobile VoLTE using the NAU8810. This is the riskiest unknown and is independent of the final codec choice.
  - *Tradeoff*: The MAX9880A is less documented in the hobbyist community than WM8960/NAU8810. The 1.8V supply adds a small amount of power-supply complexity (one LDO + one level shifter). These are minor compared to the firmware complexity of the MCU bridge alternative.

### 2026-06-28: Display Selection
- **Decision**: Select **ST7789V SPI color TFT** (2.0" 240×320, 4-wire SPI, RGB565, ~$5–8) as the display. LOCKED 2026-06-28.
  - *Rationale*: The display decision was triggered by the approaching schematic phase (display interface affects PCB routing and GPIO allocation) and the "no 5+ features blocked" principle — the display must be color-capable (photo capture rated 6, camera preview rated 6, video recording rated 5; a monochrome display would block these). Five options were evaluated:
    1. **SPI color TFT (ST7789V, 2.0" 240×320)** — SELECTED. 6 GPIO pins, 150KB framebuffer fits internal 1MB SRAM (no external SDRAM), ~$5–8, ~30–50mA with backlight (off during standby — modem dominates at 17.5mA). Zephyr `display_st7789v.c` is the most mature SPI display driver in main tree; LVGL has native ST7789 support. Camera preview ~15–25fps partial updates (adequate for rated-6 casual photos). 240×320 @ 2.0" ≈ 200 PPI — standard feature-phone resolution.
    2. **SPI color OLED (SSD1351, 1.5" 128×128)** — rejected. **Actually higher power than TFT** (~57–71mA: VCI 23–29mA + Vcc 33–42mA per datasheet — self-emissive OLED draws current per pixel; for bright text/menus it's less efficient than a backlit TFT). 3–5x more expensive ($15–24 vs $5–8). 128×128 too low for phone UI/camera preview. Not in Zephyr main tree (issue #88923 open; only out-of-tree driver nktsb/zephyr-ssd1351-driver). SSD1327 is in Zephyr but grayscale, not color. No larger color OLED options exist at reasonable prices.
    3. **E-ink (B/W or spot-color BWRY)** — **DISQUALIFIED** by "no 5+ blocked" on two grounds: (a) refresh rate makes camera preview impossible — monochrome partial refresh ~0.3s (3fps best case), color full refresh 12–22 seconds; camera preview (rated 6) needs ~15+ fps; (b) "color" e-ink at 1.5–2.4" is spot-color only (black/white/red/yellow — no blue, no green, no gradients), cannot display photos; true full-color e-ink (ACeP) starts at 6"+ and $100+. Blocks three 5+ features: photo capture (6), camera preview (6), video recording (5). E-ink's advantages (ultra-low power, bistable, sunlight readable) are genuine but don't outweigh blocking three 5+ features. Worth revisiting only if camera/photo/video are ever downgraded below 5 on the wishlist.
    4. **Parallel RGB TFT via LTDC (2.0" 320×480)** — rejected. 20 (RGB565) or 28 (RGB888) GPIO pins — eats the spare-GPIO margin on LQFP-144 needed for future peripherals (BT 4 pins, modem USB 2 pins; ~~ULPI 12 pins~~ dropped 2026-06-28). Needs external SDRAM via FMC for comfortable double-buffering (320×480 RGB565 = 300KB single, 600KB double — very tight in internal SRAM). More complex Zephyr devicetree (timings/porches/polarity). 320×480 overkill for a feature-phone UI. The smoother camera preview (parallel bus + DMA2D) does not justify the GPIO/memory/complexity cost for a rated-6 "casual photos" feature. (Note: the spare margin was 31 with ULPI planned; it is now 41 after the USB HS/ULPI Revisit dropped ULPI — see that decision below. The LTDC rejection stands regardless.)
    5. **SPI TFT 1.8" 128×160 (ST7735)** — rejected. Too low resolution for a usable phone UI. 240×320 (ST7789) is strictly better for the same effort/cost.
  - *Why ST7789V wins*: (1) Zephyr driver maturity — `display_st7789v.c` in main tree, widely used, native LVGL support; (2) GPIO-efficient (6 pins preserves spare margin for future 6+ ecosystem features); (3) no external SDRAM (150KB fits internal SRAM); (4) color-capable (satisfies "no 5+ blocked"); (5) **lower power than the color OLED alternative** (backlight off during standby, modem dominates; OLED actually draws more during use at ~57–71mA vs ~30–50mA); (6) 240×320 is the right feature-phone resolution; (7) lowest cost of viable color options (~$5–8 vs $15–24 for OLED).
  - *"No 5+ blocked" confirmation*: Photo capture (6) ✓, camera preview (6) ✓ (SPI ~15–25fps adequate), video recording (5) ✓, MP3/music (6) ✓, MMS (6) ✓, Bluetooth (6) ✓, hotspot/tethering (6) ✓, MicroSD (7) ✓, USB data (8) ✓. No 5+ feature blocked.
  - *Pre-PCB verification items*: (1) Confirm ST7789v Zephyr driver works on STM32H7 with target Zephyr version — the MIPI DBI API conversion (issue #73750) had teething issues (SPI word size, data transfer), verify on dev board before PCB. (2) Select specific panel module (ST7789V + 240×320 + SPI) from LCSC/DigiKey/Adafruit/Waveshare; confirm FPC/connector footprint. (3) Confirm panel max SPI clock (typically 40MHz) and STM32H7 SPI can drive it. (4) Plan backlight PWM on timer-output GPIO for dimming/power management (display off during standby per FR-4.3).
  - *Tradeoff*: SPI bandwidth limits camera preview to ~15–25fps partial updates (not smooth video). LTDC would be smoother but costs 14–22 more GPIO + external SDRAM + complexity. For a feature-phone camera (casual photos, rated 6), SPI is adequate. If video recording (rated 5) becomes a priority later, the LTDC path remains available on the H743 — but it would require revisiting the GPIO budget and likely the LQFP-176 package.
  - *Form factor note*: 2.0" is the primary recommendation. 2.4" is an acceptable alternative (same controller/resolution, larger physical size, lower PPI ~167 — easier to read but bulkier). Form factor is still deferred; the display choice is reasonable for an eventual phone enclosure. IPS variant preferred for viewing angle.

### 2026-06-28: USB HS / ULPI Revisit — Architecture Change to Modem-Direct USB Tethering

- **Decision**: **Drop the USB3300 ULPI transceiver / MCU USB OTG_HS path.** Use the SIM7600's own USB 2.0 HS port for ecosystem tethering (RNDIS/ECM via `AT+CUSBPIDSWITCH`), bypassing the MCU entirely. Use the MCU's USB OTG_FS (12 Mbps, built-in PHY) for all MCU-originated USB (firmware updates, file/MSC transfer, CDC ACM debug).
  - *Rationale*: Two findings drive this:
    1. **Zephyr STM32H7 USB OTG_HS + ULPI is viable but no longer needed.** ULPI support is in mainline Zephyr (PR #52772, merged Dec 2022; issue #52420 closed/completed 2024-05-14; bugs #61464 ECM-assertion and #75179 new-stack-ULPI both fixed by #104825/#76115). The STM32H747I-DISCO is a mainline board using ULPI (USB3300), confirmed working with the `cdc_acm` sample. The scary "H743 HS can't receive data" bug (#57499) was **hardware** — a floating USB3300 IO power rail; the reporter fixed it and confirmed "USB reliable and matches dev boards." So ULPI works, but it is the less-trodden path (only one mainline H7 board uses it; clock config is confusing — issue #71248 has open TODOs; ULPI pinctrl slew rate must be "high-speed"; new UDC stack is less battle-tested for ULPI than FS).
    2. **The SIM7600's own USB port does tethering directly and better.** The SIM7600 has a USB 2.0 HS (480 Mbps) device port (SIMCom hardware design PDF: "USB2.0 is up to 480Mbps"). It presents itself as a USB network adapter via RNDIS (`AT+CUSBPIDSWITCH=9011,1,1`) or ECM (`=9018,1,1`), simultaneously exposing AT/ttyUSB serial ports (composite device — confirmed via `lsusb` on Raspberry Pi). This is the module's native, best-supported tethering path (used by RPi, routers), independent of the `AT+NETOPEN` bug and independent of the project's CMUX+PPP-over-UART path. The modem's USB is free to be dedicated to tethering because the MCU controls the modem via UART.
  - *Architecture impact*: For ecosystem tethering, the car module (USB host) connects to the modem's USB (USB device) and gets LTE at 480 Mbps — no MCU bridging, no ULPI, no USB3300, no Zephyr USB HS stack, no 12 GPIO pins. The MCU USB FS bottleneck only existed when the MCU bridged LTE data to CDC ECM; that bridge is eliminated. MCU USB FS (12 Mbps) is sufficient for firmware, file transfer, and debug.
  - *Topology*: The phone has two USB device ports (MCU OTG_FS, modem USB HS) and the car module is a host. Connecting two devices to one host over one cable requires a USB hub. **Rev1 (minimal commitment)**: route the modem USB D+/D- to a connector footprint or test points; MCU USB on the main USB-C. **Future ecosystem respin**: add an internal USB 2.0 hub (e.g., USB2514, ~$1–2) so a single USB-C presents both the modem (RNDIS for LTE) and the MCU (MSC for files) to the car module — simultaneous LTE + file access over one cable.
  - *Supersedes*: The "USB HS via external ULPI transceiver (USB3300) is a board-level upgrade path preserved by the LQFP-144 package" claim (requirements.md Resolved Questions "USB mode", research-notes.md USB HS/ULPI Clarification practical path). LQFP-144 is retained — it was also chosen for its GPIO margin (now 41 spare after dropping ULPI), camera bit depth, and overall peripheral fit, not ULPI alone.
  - *Tradeoff*: Defers the hub topology to a future respin (tethering is rated 6, post-daily-driver scope). The modem USB routing footprint on rev1 keeps the door open at near-zero cost. If a future use case ever needs the MCU itself to do HS USB (not tethering), the ULPI path remains available on LQFP-144 and Zephyr supports it — but that is not a current requirement.
  - *Pre-PCB / prototype verification*: (1) Validate `AT+CUSBPIDSWITCH=9018,1,1` (ECM) and `=9011,1,1` (RNDIS) on the Waveshare NA-H HAT connected to a Linux host — confirm enumeration as a network adapter and LTE data flow (cheap HAT test). (2) Confirm whether the modem can run PPP-over-UART (MCU data) simultaneously with RNDIS-over-USB (car module data) — two PDP contexts; likely yes but unverified. See research-notes.md "USB HS / ULPI Revisit" section.

### 2026-06-28: Documentation Review — Stale Items Fixed, Missed Power/Hardware Factors Resolved
- **Decision**: Conducted in-depth cross-document review for conflicts, stale items, and missed factors. Fixed 5 stale items and resolved 6 missed hardware factors with PCB impact.
  - *Rationale*: The project has made guiding hardware choices (MCU, modem, codec, display, USB architecture all locked). Before moving to schematic design, the docs must be internally consistent and all PCB-impacting factors must be identified and resolved — not deferred.
  - *Stale items fixed*:
    1. **BOM item 4 (MCU) notes**: Removed "USB HS via ULPI transceiver (future)" — USB HS/ULPI was dropped 2026-06-28. Replaced with correct note (USB OTG_FS, LQFP-144 for GPIO margin).
    2. **research-notes.md Package Selection table**: Struck through USB HS (ULPI) column and "LQFP-144 selected to preserve USB HS option" rationale. Annotated as SUPERSEDED — LQFP-144 retained for GPIO margin (41 spare), not USB HS.
    3. **problem-definition.md constraints table**: Changed "RTOS (FreeRTOS or Zephyr)" to "Zephyr RTOS" (locked decision).
    4. **requirements.md Resolved Decisions**: Changed "RTOS (FreeRTOS or Zephyr)" to "Zephyr RTOS" (locked decision).
    5. **requirements.md Open Questions**: Moved "USB mode: device-only vs OTG" to Resolved Questions (answered: device mode sufficient, OTG host is a bonus not a requirement).
  - *Missed factors resolved (all have PCB impact — resolved now, not deferred)*:
    1. **VBAT regulator**: The docs specified a "dedicated high-current buck regulator for the modem VBAT rail." This was **incorrect** — the SIM7600 VBAT range (3.4–4.3V) matches the LiPo operating range (3.4–4.2V), so VBAT is powered **directly from the LiPo** with a separate power net (not shared with MCU 3.3V) and bulk capacitance (100–470µF). No buck regulator needed. The "dedicated rail" requirement is satisfied by a separate PCB power net, not a separate regulator. BOM item 11 removed; cost saving ~$3–6.
    2. **3.3V system rail regulator (was missing from BOM)**: The MCU, display, SD card, and codec I2S side need a stable 3.3V supply. The LiPo (3.0–4.2V) drops below 3.3V at ~40% charge — a buck-only regulator is insufficient. **Selected: TPS630201 buck-boost** (3.3V fixed, 2A, ~$3.50). Added to BOM as item 10. This was a critical gap — the BOM had no 3.3V regulator at all.
    3. **Battery fuel gauge (was missing — FR-4.2 requires battery monitoring)**: **Selected: MAX17048** I2C fuel gauge (ModelGauge, coulomb counting, ~$2.50). Shares I2C bus with MAX9880A. More accurate than ADC voltage-only (coulomb counting tracks charge during 2A modem bursts; ADC-only has ~10% error). Added to BOM as item 12.
    4. **ESD protection (was not documented)**: All external connectors need ESD protection diodes. **Selected: USBLC6-2SC6** for USB-C D+/D- (~$0.50), **ESDA6V1-5SC6** for SIM/microSD data lines (~$0.50). Added to BOM as items 23-24. Standard practice for any device with external connectors.
    5. **Display backlight driver (raw panel)**: The Waveshare module has onboard backlight circuit, but the raw panel (BOM 7b) does not. Most 2.0" ST7789V panels use 4 parallel white LEDs (common anode, ~3.1V Vf, 80mA total) — driven by 3.3V rail through current-limiting resistors + PWM-controlled N-FET. No dedicated LED driver IC needed for parallel-LED panels. Series LEDs would need a boost driver. **Pre-PCB: verify panel backlight config (parallel vs series).** Documented in constraints.md Display section and BOM item 7b notes.
    6. **Modem PWRKEY/STATUS GPIO (was implied but not documented)**: The SIM7600 requires a PWRKEY GPIO (pulse to power on/off) and a STATUS output GPIO (module ready). **Allocated 2 GPIO pins** in the GPIO budget. Documented in constraints.md MCU section. Not optional — the MCU cannot power-cycle the modem without them.
  - *MCP73831 BOM note corrected*: The original note said "May need higher-current variant for 2A modem peaks" — this conflated charging current with system load current. The charger IC charges the battery; the battery supplies modem peaks. MCP73831 (500mA) is sufficient for charging a 1200mAh battery. Note corrected in BOM.
  - *Revisit prompts archived*: All four revisit prompts (modem, codec, display, USB HS/ULPI) confirmed RESOLVED & LOCKED. Moved to `docs/revisit-prompts/archive/`. Added `docs/revisit-prompts/README.md` with archive index.
  - *BOM cost impact*: Net change approximately +$2.50 (removed ~$3–6 buck regulator, added ~$3.50 buck-boost + ~$2.50 fuel gauge + ~$1 ESD). New BOM total: ~$98–146 (was ~$94–145). NFR-5 target (< $150) still achievable.

### 2026-06-28: BOM Document Created
- **Decision**: Created `docs/bom.md` — preliminary Bill of Materials with prices and links to active online listings.
  - *Rationale*: Needed a consolidated, sourced cost estimate for the prototype BOM (NFR-5: < $150/unit) and total project budget (`docs/constraints.md`: $200–500 realistic). Prices captured 2026-06-28 from publicly listed distributor pages (ST eStore, DigiKey, LCSC, Waveshare, Adafruit, BuyDisplay, Taoglas, Analog Devices, etc.).
  - *Key findings*:
    - **Components-only BOM**: ~$91 (low) to ~$147 (high) — NFR-5 target (< $150) is achievable at the low end, tight at the high end. ~~These figures are pre-documentation-review.~~ **UPDATED 2026-06-28 (documentation review)**: ~$98 (low) to ~$146 (high) — VBAT buck regulator removed, 3.3V buck-boost + fuel gauge + ESD added. See "Documentation Review" decision below and `docs/bom.md` §5 for current figures.
    - **Dominant cost**: Cellular module (~$28–32) + JLCPCB LGA assembly (~$57–72/board). Minimizing PCB respins is the biggest cost lever.
    - **Total per unit (BOM + PCB fab/assembly)**: ~$153–269. PCB fab/assembly is correctly excluded from the NFR-5 BOM target per `docs/requirements.md`.
    - **Total project estimate**: ~$380–686 (prototyping + first PCB + 1–2 respins + tools). High end exceeds $500 if multiple respins needed.
  - *Status of components in BOM*:
    - **LOCKED**: MCU (STM32H743ZIT6, $14.61), modem (SIM7600A-H, ~$28–32), codec (MAX9880AETM+T, ~$1.70), display (ST7789V 2.0" 240×320 SPI TFT, $11.99 module / ~$5–8 raw panel), prototyping HAT (Waveshare SIM7600NA-H, $76.99).
    - **CANDIDATE** (not yet locked, revisit pending): battery (1200mAh LiPo, $9.95), charger (MCP73831, ~$1), **3.3V buck-boost (TPS630201, ~$3.50)**, 1.8V LDO (TPS7A0218, ~$0.84), **fuel gauge (MAX17048, ~$2.50)**, level shifter (TXB0104, ~$0.76), **ESD protection (USBLC6-2SC6 + ESDA6V1-5SC6, ~$1)**, USB-C connector, nano SIM socket, microSD socket, cellular antenna (Pulse W3907B0100, $5.54), earpiece speaker (Taoglas SPKM.10.8.A, ~$1.69).
    - **TBD** (deferred): loudspeaker, microphone, GNSS antenna. ~~Keypad~~ **RESOLVED 2026-06-28 — SMD tactile switches on custom PCB traces (LOCKED).** ~~High-current buck regulator for modem VBAT~~ **RESOLVED 2026-06-28 — not needed (direct from LiPo).**
  - *Open verification items carried forward*: MAX9880A PCM short-frame sync + stock; SIM7600A-H JLCPCB consignment; ST7789V Zephyr driver on STM32H7; ~~keypad design~~ **RESOLVED 2026-06-28 (SMD tactile switches, LOCKED)**; ~~buck regulator selection~~ **RESOLVED (direct from LiPo)**; 3.3V buck-boost verification; fuel gauge I2C address check; display backlight config (parallel vs series). See `docs/bom.md` §6.

### 2026-06-28: Project Kickoff
- **Decision**: Use off-the-shelf cellular module + custom MCU architecture (not designing custom modem).
  - *Rationale*: Designing a custom cellular modem is impractical. Off-the-shelf modules handle the radio protocol stack while still being challenging in PCB/firmware/mechanical design.
- **Decision**: Target LTE (not 2G/3G) for network compatibility.
  - *Rationale*: 2G and 3G networks are decommissioned or sunsetting in the US. LTE with VoLTE is the only viable long-term option.
- **Decision**: Use KiCad for PCB design.
  - *Rationale*: Open-source, well-supported, no license cost.
- **Decision**: ~~Flip/clamshell form factor with two PCBs connected via flex cable.~~
  - *Rationale*: ~~Core to the project concept. Adds mechanical and routing complexity (desired challenge).~~
  - **SUPERSEDED 2026-06-28**: Form factor deferred. Single-board first, mechanical design after electronics proven. See "Problem Definition Session" decision above. This entry is retained for history only — do not reference it as an active decision.

### 2026-06-29: MAX9880A Pre-PCB Verification — PASSED

- **Decision**: MAX9880A pre-PCB verification items are **resolved — no blockers found**. The codec is confirmed compatible with the SIM7600's PCM interface and is available for purchase.
  - *Rationale*: The docs listed two pre-PCB verification items for the MAX9880A (PCM short-frame sync compatibility + stock availability). Both are now confirmed, de-risking the final PCB codec choice.
- **Verification 1 — PCM short-frame sync: COMPATIBLE.** The MAX9880A datasheet documents "TDM Short-Sync" mode (TDM=1, FSW=0) with explicit timing parameters for slave mode (MAS=0). PCM short-frame sync is electrically identical to TDM with 1 timeslot — a short FS pulse (1 bit clock wide) marks each 16-bit sample. The MAX9880A's TDM clock range (128–2048 kHz) encompasses the SIM7600's 2048 kHz BCLK. The SIM7600 is always PCM master; the MAX9880A primary port in slave mode accepts external BCLK + FS. Register config at schematic time: TDM=1, FSW=0, MAS=0, 16-bit slot.
- **Verification 2 — Stock: AVAILABLE.** Mouser has 2,250 in stock (700-MAX9880AETM+T, $2.23 qty 1, $1.52 qty 50+). DigiKey temporarily out (normal for ADI/Maxim parts — cycles in/out). Part is active (not discontinued), manufacturer lead time 6–13 weeks. No supply risk.
- **Remaining schematic-time check**: ~~Verify SIM7600 PCM pin I/O voltage in the Hardware Design Manual.~~ **RESOLVED 2026-06-30**: SIM7600 PCM pins are **1.8V** — confirmed from the Hardware Design Manual V1.03 via direct PDF extraction (MCP `mcp-pdf` server, PyMuPDF). Two pieces of evidence: (1) **Table 32: "1.8V Digital I/O characteristics"** (page 47) — VOH = 1.35–1.8V, VIH = 1.17–2.1V, covering all digital I/O pins including PCM_OUT/IN/SYNC/CLK; (2) **§3.6.2 PCM Application Guide** (page 35) — the reference design wires PCM directly to a NAU8810 codec powered by the module's own VDD_1V8 pin (1.8V LDO output, max 50mA), with no level shifter on the PCM lines. **Conclusion: PCM lines connect directly to MAX9880A (also 1.8V) with no level shifter.** The only level shifter needed is on the MCU's 3.3V I2S lines (already planned).
- **Impact**: The MAX9880A fallback (MCU bridge with NAU8822) remains documented but is now unlikely to be needed. The codec is fully de-risked for PCB commit — all pre-PCB verification items (PCM sync, stock, voltage) are resolved.

### 2026-06-30: Reference Docs Infrastructure + PDF MCP Server

- **Decision**: Set up local reference documentation storage (`docs/reference/`) and a PDF MCP server for extracting text/tables from vendor datasheets during design work.
  - *Rationale*: As the project moves toward schematic and PCB design, the agent will need frequent, reliable access to datasheet specs (pin voltages, register addresses, timing parameters, electrical characteristics tables). Web search snippets are incomplete and sometimes wrong — the SIM7600 PCM voltage was confirmed from the actual datasheet's reference design diagram (page 35), which no search snippet exposed. Having local PDFs + an MCP extraction tool means verifiable answers from primary sources.
- **Reference docs directory**: `docs/reference/` holds vendor PDFs and saved wiki pages. Gitignored (`.gitignore` excludes everything except `README.md`) — large binaries don't belong in git. The tracked `docs/reference/README.md` indexes every file with its exact part number and download URL so the set is reproducible. Files present: SIM7600 HW Design V1.03 + V1.08, MAX9880A datasheet, STM32H743ZI datasheet + RM0433 reference manual, NUCLEO-H753ZI UM2407, ST7789V datasheet, Waveshare wiki pages (2" LCD module + SIM7600NA-H 4G HAT).
- **PDF MCP server**: `mcp-pdf` (rsp2k/mcp-pdf, PyPI, MIT license) configured in Windsurf MCP config (`~/.codeium/windsurf/mcp_config.json`), auto-imported by Devin. Provides 14 tool mixins: text extraction (PyMuPDF + pdfplumber + pypdf auto-fallback), table extraction (Camelot + pdfplumber + Tabula), OCR (Tesseract), document structure/TOC, image/SVG extraction, form handling, annotations. Tested against SIM7600 HW Design V1.03 (encrypted PDF) — extracted page 35 in 0.06s via PyMuPDF. Handles encrypted vendor PDFs that `webfetch` cannot parse.
- **Tooling**: `uv`/`uvx` (Astral v0.11.25, installed via `winget install astral-sh.uv`) manages the isolated Python environment for `mcp-pdf` — `uvx mcp-pdf` auto-creates and caches an env with the right Python version (3.11+) and all deps. No manual venv maintenance, no pollution of the Zephyr venv. MCP config uses full path to `uvx.exe` since winget PATH updates aren't visible to the Devin process mid-session.
- **Workflow**: When verifying a spec from a datasheet, call the MCP tools against the local PDFs rather than web search. See AGENTS.md "Reference Documentation & PDF MCP Server" section for details.

### 2026-07-05: Firmware Prep Started — Zephyr App Skeleton

- **Decision**: Begin firmware development while waiting for prototyping hardware. The project-log 2026-06-29 entry identified firmware as the highest-leverage use of downtime — code compiles against `nucleo_h753zi` and is flashable the day hardware arrives.
  - *Rationale*: All four major component decisions are LOCKED (modem, codec, display, keypad). All pre-PCB verification items are resolved. The Zephyr toolchain works. The only thing missing is physical hardware — but firmware architecture, devicetree config, AT parser, call state machine, and keypad scanner can all be developed and compiled without it. Front-loading this work compresses the timeline once hardware arrives.
- **Decision**: Rename project directory from `Personal Projects` to `personal_projects` (underscore, no spaces).
  - *Rationale*: Zephyr's devicetree preprocessor (C preprocessor invocation) splits file paths at spaces — `C:/Users/dengle/Documents/Personal Projects/phone/firmware/app.overlay` was parsed as two separate overlay paths ("Personal" and "Projects/phone/..."), causing a fatal build error. This is a known Zephyr limitation with paths containing spaces. Renaming the directory is the clean fix — no junctions or workarounds needed.
- **RTOS choice reviewed and confirmed**: Re-evaluated Option B (FreeRTOS + hand-integrated libraries) vs Option C (Zephyr application). The key differentiator for this project is **CMUX (GSM 07.10 mux) + PPP over UART** — the SIM7600 data path requires running AT commands and PPP simultaneously on one UART. In FreeRTOS, this is a from-scratch integration of a GSM 07.10 mux with lwIP's PPP (weeks of work, common project-staller). In Zephyr, it's a configured first-class subsystem. The existing Zephyr decision holds.
- **Zephyr adoption verified**: Linux Foundation 10-year report (March 2026) — 70% of surveyed NA organizations use Zephyr in commercial products, 69% plan to increase usage, only 1% plan to decrease. Nordic's nRF Connect SDK (the official SDK for all modern Nordic BLE chips) is Zephyr-based. Devicetree + Kconfig workflow is the same model as the Linux kernel. Learning Zephyr is a genuine career-relevant skill, not a niche tool.
- **Zephyr app architecture**: Four-file separation mirrors Linux's concern model:
  - `CMakeLists.txt` — build manifest (how to build)
  - `prj.conf` — Kconfig feature config (what subsystems to enable)
  - `app.overlay` — devicetree hardware config (what hardware exists, how it's wired)
  - `src/main.c` — application code (what the device does)
  - Each concern changes at a different rate and for a different reason. Hardware config changes when the PCB changes; feature config changes when capabilities are added; app code changes when behavior changes. Separating them means a PCB respin doesn't require touching app logic, and vice versa.
- **Skeleton created and verified**: `firmware/` directory with all four files. `prj.conf` enables shell, logging (INFO level), 4KB heap. `app.overlay` is an empty placeholder (skeleton uses only board built-ins). `main.c` is a heartbeat loop with `LOG_INF` startup message. Build succeeds: 60,476 B flash (3% of 2MB), 32,000 B RAM (3% of 1MB+). For comparison, blinky was 19,666 B / 4,544 B — the difference is shell + logging + heap, all expected.
- **Build issues encountered and fixed**:
  - (1) Space in directory path broke devicetree preprocessor → renamed `Personal Projects` to `personal_projects`.
  - (2) `CONFIG_PREEMPT_ENABLED` is not directly user-settable in Kconfig (it's derived from thread configuration) → removed from prj.conf, added explanatory comment.
  - (3) Kconfig doesn't support inline `#` comments on config value lines (`CONFIG_X=3  # comment` is parsed as value `3  # comment`) → moved comment to its own line above the config.
  - (4) `KERNEL_VERSION_STRING` macro needs an include we didn't have → removed the line (non-essential for skeleton).
- **Next steps**: Add modem UART to `app.overlay` + begin AT command parser/URC handler in `src/`. The AT command test sequences are already documented in `docs/research-notes.md` lines 624–652. The parser will be testable without hardware via mock UART input.

### 2026-06-29: Zephyr Development Environment Setup

- **Decision**: Set up the Zephyr development environment on Windows (native, not WSL) ahead of hardware delivery, to learn the west/devicetree/Kconfig workflow and enable firmware development the moment the Nucleo board arrives.
  - *Rationale*: Zephyr's learning curve is the critical path for firmware (per RTOS Selection decision — "the steep learning curve is a feature, not a bug"). Setting it up while waiting for parts is the highest-value use of downtime. Native Windows (not WSL) per Zephyr docs — WSL flashing is not supported.
- **Environment installed**:
  - **Host tools** (via winget): CMake 4.3.3, Ninja 1.13.2, gperf 3.1, dtc 1.6.1, 7-Zip 26.01, Python 3.12.10 (separate from system Python 3.10).
  - **Zephyr workspace**: `C:\Users\dengle\zephyrproject` (external to the phone repo — multi-GB tree). Python 3.12 venv at `zephyrproject\.venv` (contains west 1.5.0 + all Zephyr Python deps).
  - **Zephyr source**: main branch (version 4.4.99 / `v4.4.0-6582-g191d4b5cada0`), fetched via `west init` + `west update` (all modules including STM32 HAL).
  - **Zephyr SDK**: v1.0.1 at `C:\Users\dengle\zephyr-sdk-1.0.1`. ARM toolchain (`arm-zephyr-eabi-gcc` 14.3.0, GNU ld 2.43.1) confirmed working. Host tools (QEMU/OpenOCD) not available for Windows from SDK — will install OpenOCD separately when needed for flashing via ST-Link.
  - **CMake package registry**: Zephyr and Zephyr-sdk registered via `west zephyr-export` + SDK setup.
- **Verification**: Built `samples/basic/blinky` for `nucleo_h753zi` (the exact prototyping board). Build succeeded: 159/159 compile steps, `zephyr.elf` linked, FLASH 19660 B (0.94%), RAM 4544 B (0.87%). Toolchain is fully operational.
- **Convenience script**: `scripts/activate-zephyr.ps1` in the phone repo — dot-source it (`. .\scripts\activate-zephyr.ps1`) to refresh PATH and activate the venv in a new terminal.
- **Known limitation**: Zephyr SDK Windows host tools (QEMU, OpenOCD) are "not available yet" per the SDK setup. For flashing the Nucleo (onboard ST-Link), install OpenOCD separately (e.g., via winget or xpack-openocd) before Phase 2 bring-up. Building works now; flashing needs this one extra step.
- **Next firmware steps** (no hardware needed): learn devicetree overlays + Kconfig for STM32H7, write AT command parser/URC handler, call state machine, keypad matrix scanner — all compilable against the `nucleo_h753zi` board target and flashable the day the board arrives.

### 2026-06-28: Keypad Selection
- **Decision**: **SMD tactile switches on custom PCB traces** for the final PCB keypad. **LOCKED.**
  - *Rationale*: Three switch technologies were evaluated for the final PCB: (1) SMD tactile switches on PCB traces — snappy, mechanical, individual parts, easy to source from DigiKey/Mouser/LCSC, no custom tooling; (2) conductive-rubber (silicone dome) keypad over PCB pad pairs — soft dome feel, sealed, most "phone-like" (classic feature-phone construction, e.g. Nokia 3310), but requires a custom silicone dome sheet (~$50–150 mold run from Chinese vendors, harder to source in prototype qty); (3) membrane keypad (PET/FPC stack) — thinnest, fully sealed, but flat feel and harder to source in small qty. SMD tactile selected for simplicity, sourcing, and alignment with the custom-PCB learning goal. The key matrix (5×4 = 9 GPIO for ~20 keys) is identical regardless of switch technology, so the schematic/GPIO allocation is unaffected by this choice.
  - *Key layout*: ~17–19 keys minimum per FR-2.1/FR-2.3 — 12 numeric (0–9, *, #) + 2 call/end + 3–5 nav (up/down/select or D-pad). 5×4 matrix (20 keys) cleanly fits with 1 spare.
  - *GPIO*: 5×4 matrix = 9 GPIO pins. Non-issue with 41 spare GPIO on LQFP-144.
  - *Cost*: ~20 SMD tactile switches at ~$0.05–0.10 each = ~$1–2. Cheaper than membrane or conductive-rubber alternatives.
  - *Tradeoff*: SMD tactile switches are not dust/water sealed (unlike conductive-rubber or membrane). Acceptable for a personal prototype/daily-driver; sealing can be revisited if needed via a silicone overlay sheet on top of the SMD switches.
- **Decision**: **Off-the-shelf 4×4 matrix keypad module + loose tactile buttons** for Phase 2 prototyping.
  - *Rationale*: A $2–5 4×4 matrix membrane module plus ~6–8 loose 6×6mm tactile buttons on a breadboard (for call/end/nav) enables immediate firmware development (matrix scan + debounce) with zero custom hardware. The electrical interface (matrix rows/cols → MCU GPIO) is identical to the final PCB, so no firmware rework when moving to the custom board.
  - *Tradeoff*: The prototyping keypad feel is unrelated to the final PCB keypad feel (SMD tactile). This is acceptable — Phase 2 validates firmware, not mechanical feel. Keypad feel is a Phase 7 (mechanical/enclosure) concern.

### 2026-07-18: UI Design Session

- **Context**: MVP complete (corrected criteria). Keypad + arbitrary dialing working on hardware. User wants to fix the UI and map out everything that should be part of it before adding more features (signal indicator, SMS, contacts). This is a design session — no code written yet, just decisions recorded.
- **Decision**: Both firmware and PCB schematic tracks in parallel. UI/firmware work in sessions; PCB schematic in between. Rationale: they're different kinds of work and don't block each other. The 2026-07-13 roadmap put PCB schematic next; this session adds a parallel firmware track (daily-driver features) without displacing PCB work.
- **Decision**: Full daily-driver UI screen map committed. Screens: idle/dialer, main menu, contacts (list/detail/add-edit), messages (list/conversation/compose), settings (deferred), in-call, incoming call. See `docs/ui-design.md` for the full map with ASCII diagram.
- **Decision**: Input model for the prototype 4×4 keypad — 2/8 as up/down in menus (Nokia-style feature-phone convention), A=select, B=back. Digits 2/8 double as nav in menu context and as digits in dialer context. Rationale: familiar to anyone who used a feature phone; the final PCB's 5×4 matrix will have dedicated nav keys, so this is a prototype-only mapping. Full key mapping table in `docs/ui-design.md`.
- **Decision**: Visual style — 80s sci-fi HUD as an explorative starting point (amber/orange on black, chunky borders, all-caps, vector icons, industrial/utilitarian vibe). User explicitly said "this will be an explorative process" — the visual style is not a fixed spec, it's a starting direction to iterate from on hardware. Three starting points were proposed (CRT terminal, cyberpunk neon, 80s sci-fi HUD); user chose HUD. See `docs/ui-design.md` Visual Style section.
- **Decision**: Contact storage — Zephyr settings subsystem for now (key-value in existing 256KB `storage_partition`). Simplest option, no filesystem setup, good for dozens of contacts. Rationale: user said "simplest version for now, and decide proper/better location later." Migrate to littlefs when we need SMS archives / call logs / hundreds of contacts. SMS storage leans on the SIM7600's own message store (read on demand via `AT+CMGR`).
- **New doc**: `docs/ui-design.md` created — screen map, persistent elements (status bar / content / soft-key labels), input model table, visual style direction, implementation notes. Living document — screen map and input model are stable; visual style evolves.
- **Next step**: Build the idle/dialer screen in the 80s sci-fi HUD style as the first explorative iteration. Get it on hardware, react, iterate. Then proceed to signal indicator, then menu skeleton, then contacts, then SMS.

### 2026-07-18: MVP Criteria Correction

- **Decision**: Move battery indicator and signal indicator out of the MVP column in `docs/problem-definition.md` (both changed from ✅ to ❌ in MVP, remain ✅ in Daily Driver).
  - *Rationale*: The MVP narrative and success criterion have always been "make and receive voice calls with intelligible audio on a US LTE network" — nothing more. The success criterion was achieved 2026-07-13. The table listing battery + signal indicators as ✅ in the MVP column was an inconsistency that contradicted the narrative and the success criterion. Signal indicator is daily-driver polish (doable now via `AT+CSQ`, but not required for MVP). Battery indicator is daily-driver polish blocked on PCB phase (MAX17048 fuel gauge needs battery power path). Neither blocks MVP completion.
  - *Impact*: **MVP is now fully complete** per the corrected criteria. All MVP-column items (make calls, receive calls, enclosure=test jig) are done. The 2026-07-13 roadmap entry that planned "complete all doable-now MVP items" was based on the inconsistent table — signal indicator is still worth implementing as a daily-driver feature, but it's no longer framed as MVP completion work.

### 2026-07-18: Keypad Integration — gpio-kbd-matrix + Dial String UI

- **Context**: Post-MVP roadmap (2026-07-13) identified 4×4 matrix keypad + arbitrary-number dialing as the next step. User has the Adafruit 4×4 membrane keypad (PID 3844) on hand. This session implements keypad input, dial string accumulation, and replaces the hardcoded `CALL_NUMBER` with a user-typed number.
- **Decision**: Use Zephyr's built-in `gpio-kbd-matrix` input driver (not a manual matrix scan).
  - *Rationale*: The driver handles scanning, per-key debouncing, ghost-key detection, and idle wake (interrupt mode) in its own thread. The app just receives `(col, row)` events via an input callback. This is the idiomatic Zephyr approach (the old `kscan` API was renamed to `input` in recent Zephyr), teaches the input subsystem (aligned with the project's learning goal), and requires **no firmware rework when moving to the custom PCB** — only the devicetree GPIO pins change. A manual scan would reinvent debounce/ghost-detection and be thrown away at PCB time.
- **Decision**: Pin assignment for the 4×4 matrix on the Nucleo Arduino headers (user-selected):
  - Rows: R1→D3 (PE13), R2→D4 (PE14), R3→D5 (PE11), R4→D6 (PE9) — inputs, pull-up, active-low
  - Cols: C1→A1 (PC0), C2→A2 (PC3), C3→A3 (PB1), C4→D2 (PG14) — outputs, active-low
  - *Rationale*: All 8 pins are free (not used by SPI1 display, LPUART1 modem, I2C1, USART3 console, B1 button, or ADC1 on A0). A1–A3 are Arduino analog-header pins but used as digital GPIO (the keypad needs digital I/O, not ADC). All 4 row pins support EXTI interrupts (required for the driver's `idle-mode = "interrupt"` wake).
- **Devicetree**: Added `kbd_matrix` node to `app.overlay` with `compatible = "gpio-kbd-matrix"`, 4 `row-gpios` + 4 `col-gpios`, `debounce-down-ms = <10>`, `debounce-up-ms = <20>`, `poll-timeout-ms = <100>`, `idle-mode = "interrupt"`. Added `CONFIG_INPUT=y` to `prj.conf` (the driver auto-selects via `DT_HAS_GPIO_KBD_MATRIX_ENABLED`, but the explicit config documents intent). Build verified: the driver bound to the node (`gpio_kbd_matrix_cb_0` + `gpio_kbd_matrix_init` in the linker map), devicetree compiled with correct pin flags (`0x11` = `GPIO_PULL_UP | GPIO_ACTIVE_LOW` on rows, `0x1` = `GPIO_ACTIVE_LOW` on cols).
- **Firmware architecture** (main.c):
  - **Input callback** (`keypad_callback`): registered via `INPUT_CALLBACK_DEFINE(DEVICE_DT_GET(DT_NODELABEL(kbd_matrix)), ...)`. Runs in the input subsystem thread (not ISR, not main loop). Captures `INPUT_ABS_X` (col) and `INPUT_ABS_Y` (row) on the ABS events, then on `INPUT_BTN_TOUCH` press (value=1, sync=true) looks up `keymap[row][col]` and puts the character into `key_msgq` via `k_msgq_put(..., K_NO_WAIT)`.
  - **Key event queue** (`K_MSGQ_DEFINE(key_msgq, sizeof(char), 16, 4)`): passes key characters from the input callback (driver thread) to the main loop (owns LVGL + modem). This is the thread-safety boundary — the callback never touches LVGL or the modem UART directly, avoiding cross-thread LVGL/modem calls. `k_msgq` is designed for cross-thread communication.
  - **Keymap** (`keymap[4][4]`): maps `(row, col)` → character per the Adafruit 4×4 membrane layout: row 0 = `1 2 3 A`, row 1 = `4 5 6 B`, row 2 = `7 8 9 C`, row 3 = `* 0 # D`.
  - **Dial string** (`dial_string[21]` + `dial_len`): replaces the hardcoded `CALL_NUMBER`. Digits `0-9/*/#` append (IDLE state only, max 20 chars), `C` backspaces, `A` triggers `ATD<dial_string>;` via `snprintf`, `B` hangs up / cancels / rejects, `D` is a no-op (menu, future).
  - **handle_key(char key)**: replaces `handle_button()`. State-dependent key actions: IDLE → digits append / A=call / C=backspace; CALLING → B=cancel; RINGING → A=answer / B=reject; CONNECTED → B=hang up.
  - **B1 button removed**: the Nucleo's user button (PC13) was the previous call control. Now redundant — A/B keys do the same things. Removed `gpio_dt_spec`, `gpio_callback`, `button_isr`, button init, and button polling from the main loop. Also removed `#include <zephyr/drivers/gpio.h>` (no longer needed — the input driver handles GPIO internally).
  - **Main loop**: drains `key_msgq` first (`k_msgq_get` with `K_NO_WAIT`), then processes modem URCs (`modem_readline`), then `lv_timer_handler()`. All LVGL and modem access stays in the main loop — no cross-thread issues.
- **Thread safety**: The input callback runs in the input subsystem thread. It only does `k_msgq_put()` (thread-safe by design). All LVGL calls, modem UART calls, and `call_state` transitions happen in the main loop (which drains the queue and processes URCs). `call_state` is read/written only in the main loop now — no race. The only cross-thread data path is `key_msgq`, which is purpose-built for that.
- **Build result**: Firmware compiles clean (exit 0, no warnings). Flash 333,956 B (15.92%), RAM 56,980 B (10.87%). Delta from before: +292 B flash (keymap, callback, handle_key, msgq plumbing), +80 B RAM (msgq buffer + dial string). The input driver + matrix library add ~5 KB flash (already counted in the first build with the overlay).
- **Not yet tested on hardware**: The firmware compiles and the driver binds, but the keypad wiring + functional test (press key → digit appears on display → type number → press A → call places) is the next step. The user has the keypad on hand.
- **Hardware test — FIXED and VERIFIED same day**: First flash showed `<err> input_gpio_kbd_matrix: Pin 0 interrupt configuration failed: -16` (EBUSY). Root cause: STM32 EXTI line conflict — our row 0 is PE13 (needs EXTI13), but the board DTS defines the B1 user button on PC13 (also EXTI13). STM32 EXTI lines 0–15 are port-shared (only one port per line), and the `gpio-keys` driver claimed PC13/EXTI13 first at boot, blocking PE13. Fix: added `&user_button { status = "disabled"; }` to `app.overlay` — we removed B1 from app code (keypad A/B replaced it), so disabling the board's button node frees EXTI13 for PE13. Build verified the `status = "disabled"` propagated to the compiled DTS. After reflash: clean boot, no `<err>` lines, keypad fully functional.
- **Functional test confirmed**: User typed `6077936142` (with a `123` typo cleared via C/backspace), pressed A → `ATD6077936142;` → `VOICE CALL: BEGIN` (connected in ~10s) → pressed B → `AT+CHUP` → `VOICE CALL: END: 000044`. **Real VoLTE call to an arbitrary user-typed number, no PC involvement, controlled entirely from the keypad + display.** This is the standalone-dialer milestone — the phone is no longer a hardcoded test jig.
- **Remaining MVP items**: Signal indicator (`AT+CSQ` polling + bars on idle screen) is the last doable-now MVP item. Battery indicator remains PCB-phase (MAX17048 fuel gauge needs battery power path — HAT's regulator prevents validation).

### 2026-07-13: Post-MVP Roadmap Planning
- **Context**: MVP success criterion achieved (place/receive VoLTE calls with audio). Reviewed remaining MVP feature table items and next-phase priorities with user.
- **MVP status audit**: The MVP feature table in `docs/problem-definition.md` lists four MVP items: make calls (done), receive calls (done), battery indicator (not done — blocked on PCB/MAX17048 fuel gauge), signal indicator (not done — doable now via `AT+CSQ`). Caller ID (`AT+CLIP=1`) is NOT a listed MVP item — it's an enhancement to "receive calls" (success criterion already met with hardcoded number on RINGING screen).
- **Decision**: Battery indicator deferred to PCB phase. Verified from SIM7600 HW Design Manual V1.03 §3.1 (Table 6: VBAT 3.4–4.2V, 2A peaks) and Waveshare HAT wiki that the current Nucleo+HAT setup has no battery path — the HAT's onboard regulator sits between USB 5V and the module's VBAT pins, and `AT+CBC` would monitor the regulator output (always ~3.8V), not a battery. The fuel gauge (MAX17048) needs to be in the battery's power path, which neither dev board provides. Power architecture (VBAT direct from LiPo, TPS630201 buck-boost, MAX17048, MCP73831) must be validated on the custom PCB following SIMCom's §3.1 reference circuit. No battery hardware ordered for prototyping.
- **Decision**: Next session (~90 min) targets completing all doable-now MVP items in one go:
  1. Wire 4×4 matrix keypad module to Nucleo GPIO (4 row + 4 col = 8 pins)
  2. Devicetree `kscan-matrix` node in app.overlay (Zephyr built-in driver handles debouncing)
  3. Keypad → dial string: 0-9/\*/# accumulate digits, A=Call (`ATD<number>;`), B=End (`AT+CHUP`), C=Backspace, D=Menu (future)
  4. LVGL dialer screen: typed digits shown on display, replaces hardcoded `CALL_NUMBER`
  5. Signal indicator: poll `AT+CSQ` periodically, render signal bars on idle screen
  - After this, MVP feature table is complete except battery indicator (PCB-phase).
- **Decision**: After MVP-complete session, priority is PCB schematic design (KiCad) — all component selections are locked. During PCB fab/assembly wait, parallel firmware track: AT command parser refactor (separate echo/response/URC), UART stress test (open item in requirements.md), multi-thread architecture (modem/UI/keypad threads), then daily-driver features (SMS, contacts).
- *Rationale*: The 2026-07-13 session demonstrated high throughput (7 milestones in one session). Keypad + arbitrary calling + signal indicator is achievable in a single ~90 min session. PCB schematic is the critical path to a standalone device — starting it immediately after MVP completion keeps the project moving toward the daily-driver goal. Filling the PCB fab lead time with firmware hardening avoids dead time.

## Phase Breakdown & Effort Estimate

### Phase 1: Research & Component Selection (~2-3 weeks, part-time)
- Deep-dive into cellular modules, compare datasheets
- Select MCU, display, cellular module, audio components
- Order dev boards / breakout boards for initial prototyping
- Get a prepaid SIM and test module with AT commands on a breadboard
- **Deliverable**: Component shortlist + working AT command proof-of-concept on breadboard

### Phase 2: HAT-Based Prototype (~2-4 weeks, part-time)
- Validate SIM7600 VoLTE + audio on **Waveshare SIM7600NA-H 4G HAT** (~$89, has B71 + NAU8810 codec) via PC (USB-to-UART) before any MCU work
  - **First**: validate simultaneous VoLTE + data tethering (PDP context management) — critical for ecosystem. Use AT command sequence in research-notes.md modem revisit findings. If this fails, it blocks a future feature (not MVP) — see modem revisit fallback options.
  - Verify/update SIM7600 firmware to VoLTE-capable version
  - Test call audio path (NAU8810 codec on HAT)
  - Validate B71 performance (NA-H HAT has B71, unlike the G-H HAT)
- Then wire MCU (STM32H743 dev board) + HAT + display + keypad
- Write firmware: UART driver for AT commands, call state machine, basic UI
- Make a test phone call
- **Note**: The SIM7600 is LGA and cannot be breadboarded directly — the Waveshare HAT is the prototyping platform. The MCU and peripherals can be on breadboard/perfboard connected to the HAT.
- **Deliverable**: Working phone call from HAT + MCU setup

### Phase 3: Schematic Design (~2-4 weeks, part-time)
- Design full schematic in KiCad
- Power management circuit (battery charging, regulation, module power)
- Audio circuit (mic, speaker, codec or module direct)
- Display and keypad interfacing
- **Deliverable**: Complete schematic, reviewed

### Phase 4: PCB Layout (~3-5 weeks, part-time)
- Board layout (MCU, cellular module, power, keypad, SIM, mic, display, audio)
- RF trace impedance control for antenna
- DRC, manufacturing prep (Gerbers, BOM, pick-and-place)
- **Deliverable**: PCB files sent to fab

### Phase 5: Assembly & Board Bring-up (~2-4 weeks)
- Receive PCBs, solder components
- Power on, verify rails, basic MCU blink
- Bring up cellular module, verify network registration
- Bring up display, keypad, audio
- **Deliverable**: Functional bare PCB phone (no enclosure)

### Phase 6: Firmware Development (~4-8 weeks, part-time)
- Full UI implementation (menus, contacts, call screen)
- Power management policies (sleep, wake, modem power states)
- Call handling robustness (error recovery, network loss)
- Contact storage (flash/EEPROM)
- **Deliverable**: Feature-complete firmware

### Phase 7: Mechanical Design & Enclosure (~3-6 weeks, part-time)
- 3D model the enclosure in CAD
- Design form factor (flip, candybar, etc. — decision made by this phase)
- Design keypad integration
- 3D print / CNC iterations, fit-check with PCB
- **Deliverable**: Functional enclosure, assembled phone

### Phase 8: Integration & Testing (~2-4 weeks)
- Full assembly
- Test calls, battery life, durability
- Fix issues discovered during integration
- **Deliverable**: Working phone prototype

### Total Estimate
- **Conservative estimate**: 4-6 months working part-time (evenings/weekends)
- **Aggressive estimate**: 2-3 months with focused effort
- **Realistic estimate**: 5-8 months with natural pacing, learning curves, and iteration

### Key Risk Areas (likely to extend timeline)
1. **RF/antenna**: First PCB may have antenna issues requiring a respin.
2. **Cellular module bring-up**: Subtle AT command / VoLTE configuration issues.
3. **Mechanical/form factor**: Enclosure fit, keypad feel, 3D print/CNC iterations.
4. **Power management**: Battery life tuning, peak current handling.

## Progress Tracking

| Date | Milestone | Status |
|------|-----------|--------|
| 2026-06-28 | Project definition & documentation started | In Progress |
| 2026-06-28 | Modular ecosystem vision documented; USB interconnect constraint added to hardware selection | Done |
| 2026-06-28 | Cellular module selected: SIMCom SIM7600 (Architecture A: Cat 4 + MCU + codec) | Done |
| 2026-06-28 | RTOS selected: Zephyr (built-in USB/networking/FS/LVGL/PM subsystems) | Done |
| 2026-06-28 | MCU selected: STM32H743ZI (LQFP-144, 480MHz Cortex-M7) | Done |
| 2026-06-28 | "No 5+ features blocked" principle adopted for component selection | Done |
| 2026-06-28 | Package decision validated: LQFP-144 over LQFP-176 (GPIO budget, camera bit depth, FMC analysis) | Done |
| 2026-06-28 | Video recording analysis: 8-bit DVP sufficient for VGA@30fps raw, HD@30fps with JPEG | Done |
| 2026-06-28 | Documentation review: resolved stale open questions, marked flip decision superseded, added VBAT/standby-power/NITZ/SIM7600-firmware/codec-audio-path notes, added component checklist | Done |
| 2026-06-28 | Modem revisit: SIM7600 selection confirmed; switch prototyping HAT to SIM7600NA-H (B71 validation); VoLTE+data is first HAT test; standby power figure corrected (17.5 mA idle/DRX, not 3 mA); NFR-3 updated (LGA assembly reality) | Done |
| 2026-06-28 | Modem revisit (second round): SIM7600 **locked**. LARA-R6401 disqualified (911 not supported). EC25-AF evaluated as fallback (T-Mobile voice cert, B71, but no codec breakout). PDP conflict confirmed AT+NETOPEN-specific (project uses CMUX+PPP). Simultaneous VoLTE+data dropped as a requirement (not needed for MVP/daily driver; pause-data fallback acceptable). | Done |
| 2026-06-28 | Codec selected: **MAX9880A** (dual-port codec, TQFN-48, ~$1.70). SIM7600 PCM→codec primary (voice), MCU I2S→codec secondary (music). MCU NOT in voice path. Common single-port codecs (WM8960, NAU8810, etc.) cannot do both simultaneously — original "unified audio path" claim was false. Fallback: MCU bridge with NAU8822 if MAX9880A unavailable. HAT prototyping unchanged. | Done |
| 2026-06-28 | Display selected: **ST7789V SPI color TFT** (2.0" 240×320, RGB565, ~$5–8). 6 GPIO pins, 150KB framebuffer fits internal SRAM (no external SDRAM). Zephyr `display_st7789v.c` is most mature SPI display driver in main tree. Five options evaluated: ST7789V TFT (selected), SSD1351 color OLED (rejected — actually higher power ~57–71mA, 3–5x cost, 128×128 too low, not in Zephyr), e-ink (DISQUALIFIED — blocks camera preview/photo/video 5+ features via refresh rate + spot-color limitation), LTDC parallel RGB (20–28 GPIO, needs external SDRAM, overkill res), ST7735 1.8" (too low res). Color-capable — satisfies "no 5+ blocked". Pre-PCB: verify ST7789v driver on STM32H7 + target Zephyr version. | Done |
| 2026-06-28 | USB HS/ULPI revisit: **Drop USB3300 ULPI transceiver.** Zephyr STM32H7 ULPI is viable (mainline since Dec 2022, PR #52772; bugs fixed) but no longer needed — the SIM7600's own USB 2.0 HS port (480 Mbps) does tethering directly via RNDIS/ECM (`AT+CUSBPIDSWITCH`), bypassing the MCU. MCU USB OTG_FS (12 Mbps) is sufficient for firmware/files/debug. Rev1: route modem USB to connector footprint/test points. Future ecosystem respin: internal USB 2.0 hub (USB2514) for single-cable simultaneous LTE + file access. LQFP-144 retained (GPIO margin, not ULPI). | Done |
| 2026-06-28 | **Documentation review**: Fixed 5 stale items (BOM MCU note, package selection table, firmware wording, USB mode question). Resolved 6 missed PCB-impacting factors: VBAT direct-from-LiPo (no buck regulator), 3.3V buck-boost (TPS630201, was missing), fuel gauge (MAX17048, was missing), ESD protection (USBLC6-2 + ESDA6V1), backlight driver note, modem PWRKEY/STATUS GPIO. MCP73831 note corrected. Revisit prompts archived. BOM ~$98–146 (was ~$94–145). | Done |
| 2026-06-28 | Keypad selected: **SMD tactile switches on custom PCB traces** (final PCB, LOCKED). 5×4 matrix = 9 GPIO for ~20 keys (12 numeric + 2 call/end + 3–5 nav + 1 spare). Three options evaluated: SMD tactile (selected — simple, cheap, easy to source), conductive-rubber (phone-like feel but custom silicone tooling), membrane (sealed but flat feel, hard to source). Phase 2 prototyping: off-the-shelf 4×4 matrix module + loose tactile buttons. | Done |
| 2026-06-29 | **MAX9880A pre-PCB verification PASSED.** PCM short-frame sync: CONFIRMED COMPATIBLE (TDM short-sync slave mode matches SIM7600 PCM master; 128–2048 kHz clock range encompasses 2048 kHz BCLK). Stock: AVAILABLE (Mouser 2,250 in stock, $2.23 qty 1; DigiKey temp out, part active). Schematic-time: verify SIM7600 PCM pin I/O voltage. Fallback (NAU8822 MCU bridge) now unlikely needed. | Done |
| 2026-06-29 | **Zephyr development environment set up** (Windows native). Host tools (cmake, ninja, dtc, gperf, python 3.12) via winget. Zephyr workspace at `~/zephyrproject` (main branch, v4.4.99). Zephyr SDK 1.0.1 with `arm-zephyr-eabi-gcc` 14.3.0. Built `samples/basic/blinky` for `nucleo_h753zi` — toolchain verified end-to-end. Convenience script: `scripts/activate-zephyr.ps1`. OpenOCD for flashing TBD (SDK Windows host tools not available). | Done |
| 2026-06-30 | **SIM7600 PCM pin voltage RESOLVED: 1.8V confirmed.** Direct PDF extraction from Hardware Design Manual V1.03 (via MCP `mcp-pdf` server): Table 32 "1.8V Digital I/O characteristics" (VOH 1.35–1.8V) covers PCM pins; §3.6.2 reference design wires PCM directly to NAU8810 codec powered by VDD_1V8 with no level shifter. PCM lines connect directly to MAX9880A — only the MCU I2S lines need level shifting (already planned). **All MAX9880A pre-PCB verification items now fully resolved.** | Done |
| 2026-06-30 | **Reference docs infrastructure + PDF MCP server set up.** `docs/reference/` directory created for vendor datasheets/manuals (gitignored — only `README.md` index is tracked). Local PDFs: SIM7600 HW Design V1.03 + V1.08, MAX9880A datasheet, STM32H743ZI datasheet + RM0433 ref manual, NUCLEO-H753ZI UM2407, ST7789V datasheet, Waveshare wiki pages (2" LCD + SIM7600NA-H HAT). MCP `mcp-pdf` server (rsp2k/mcp-pdf, PyPI) configured in Windsurf MCP config — provides PDF text/table/image/SVG extraction via PyMuPDF with auto-fallback. `uv`/`uvx` installed via winget for isolated Python env management. | Done |
| 2026-07-05 | **Firmware prep started — Zephyr app skeleton created and builds.** Began firmware development while waiting for prototyping hardware (Waveshare HAT + Nucleo + SIM). Session covered: (1) firmware concept definition (hardware-coupled, lowest software layer, non-volatile residency); (2) RTOS choice review — Option B (FreeRTOS + hand-integrated libs) vs Option C (Zephyr app) — Zephyr's built-in CMUX+PPP subsystem is the key differentiator for this project; (3) Zephyr adoption analysis — 70% of surveyed NA orgs use Zephyr in commercial products (Linux Foundation 10-year report, March 2026), Nordic nRF Connect SDK is Zephyr-based, devicetree+Kconfig workflow transfers to Linux kernel development; (4) Zephyr app architecture explained — four-file separation (CMakeLists.txt/build, prj.conf/features, app.overlay/hardware, src/code) mirrors Linux's concern separation; (5) `firmware/` directory created with skeleton: `CMakeLists.txt`, `prj.conf` (shell+logging+heap), `app.overlay` (empty placeholder), `src/main.c` (heartbeat loop). Build verified: 60,476 B flash (3% of 2MB), 32,000 B RAM (3% of 1MB+). Two build issues fixed: (a) space in "Personal Projects" path broke Zephyr's devicetree preprocessor — renamed to `personal_projects`; (b) Kconfig doesn't support inline `#` comments on config value lines. Next: add modem UART to overlay + AT command parser. | Done |
| 2026-07-12 | **Phase 2 HAT bring-up: SIM7600NA-H modem alive and registered on Mint LTE.** All prototyping hardware received. HAT standalone testing (PC-connected, no MCU) started. Key findings: (1) HAT auto-powers on (new version — PWR/3V3 jumper shorted by default, no PWRKEY press needed); (2) AT command port is the SIM7600's own USB port (not the CP210x UART bridge) — requires SIMCom USB drivers ([SIM7600X driver V1.0.2](https://files.waveshare.com/upload/2/24/SIMCOM_Windows_USB_Drivers_V1.0.2.zip)); CP210x port did not respond to AT commands; (3) SIM7600 enumerates multiple virtual COM ports — AT command port is the lowest-numbered one; COM port numbers are dynamic per connection (can be fixed via Device Manager → Port Settings → Advanced → COM Port Number); (4) PuTTY serial terminal works at 115200 baud with CR line endings (not CR+LF); local echo can be forced on if modem echo is insufficient. **Modem status**: Firmware LE20B02SIM7600NA (older than documented LE20B04/05 — VoLTE support unverified, may need firmware update); IMEI 864688041535110; SIM READY; CSQ 19 (decent); CEREG 0,1 (registered home); COPS "Mint" AcT=7 (LTE confirmed). `AT+CPSI?` returns ERROR on this firmware (not supported). Next: test VoLTE configuration (`AT+voltesetting=1`), verify call audio path, place test call. | Done |
| 2026-07-12 | **Phase 2 VoLTE VALIDATED: First call placed on Mint LTE.** `AT+voltesetting=1` returned OK on LE20B02 — **no firmware update needed** (LE20B02 supports VoLTE despite being older than documented LE20B04/05). PDP contexts: cid=1 "fast.t-mobile.com" (data), cid=2 "ims" (VoLTE IMS, auto-created), cid=3 "sos" (emergency/911, flag=1), cid=4 "tmus" (T-Mobile US). `AT+CNSMOD: 0,8` confirms LTE mode (not CSFB to 3G/2G). Test call `ATD6078821755;` → `VOICE CALL: BEGIN` → `VOICE CALL: END: 000006` — call connected and ended normally. **VoLTE is working on LE20B02 with Mint/T-Mobile.** This validates the hardest part of the project (cellular + VoLTE on a real US carrier) with zero custom hardware. Next: verify call audio path (NAU8810 codec on HAT), receive incoming call, then MCU integration. | Done |
| 2026-07-12 | **Phase 2 AUDIO VALIDATED: Full duplex VoLTE audio works both directions.** Placed and received test calls — audio works both ways (mic and speaker) through the HAT's NAU8810 codec. The entire voice path (SIM7600 PCM → NAU8810 codec → speaker/mic) is validated on the HAT. **Key risk #1 (VoLTE configuration) and the audio path are now fully de-risked** before any MCU or PCB work. Call control commands confirmed: `ATD<number>;` (dial), `ATH` (hang up), `ATA` (answer). Next: SMS test, then MCU integration. | Done |
| 2026-07-12 | **Phase 2 SMS VALIDATED: Send and receive both work.** `AT+CMGF=1` (text mode) → `AT+CMGS="<number>"` → type message → Ctrl+Z (0x1A) to send. Received SMS read via `AT+CMGR=<index>` after `+CMTI` URC notification. SMS is post-MVP but validates another AT command path before MCU work. **Key findings**: (1) Phone numbers must include `+1` country code prefix (E.164 format) — `AT+CMGS="60788221755"` fails with ERROR, `AT+CMGS="+16078821755"` works; this is a Mint/T-Mobile network requirement. (2) `AT+CMGF=1` must be re-run after each power cycle (text mode is not persistent). (3) After power cycle, wait for `AT+CEREG?` to show `0,1` (registered) before SMS will work. **PuTTY gotcha**: arrow keys send escape sequences that corrupt the modem's command parser — use backspace only, never arrow keys. If modem gets confused, send `AT` to resync or power-cycle the HAT. | Done |
| 2026-07-12 | **Phase 2 GNSS VALIDATED: SIM7600 GPS works with valid fix.** `AT+CGPS=1` → `AT+CGPSINFO` returned valid position (42.35°N, -76.43°W, Ithaca NY area) in ~60 seconds cold start. Altitude 501.8m, stationary. GNSS works while module is registered on network (resolves open research question — sufficient for ecosystem car module use case). `AT+CBAND?` not supported on LE20B02 (like `AT+CPSI?`); `AT+CNMP?` returns 2 (automatic mode). CMUX tested: `AT+CMUX=0` returns OK (multiplexer enabled — full PPP data testing deferred to MCU/Zephyr where the CMUX subsystem handles virtual channels). | Done |
| 2026-07-13 | **MCU HELLO WORLD: Firmware flashed and running on NUCLEO-H753ZI.** Full edit→build→flash→run loop verified. Firmware skeleton (60,492 B flash, 3% of 2MB) flashed via ST-Link drag-and-drop (copy `zephyr.bin` to mounted Nucleo USB drive — no OpenOCD needed). Serial output confirmed at 115200 baud on ST-Link VCP: `[inf] phone: Phone firmware starting...` + `[inf] phone: alive` every 5s. Zephyr shell (`uart:~$`) also live on same UART — runtime introspection available (help, kernel threads, kernel uptime). Both the log heartbeat thread and shell thread run concurrently via RTOS scheduler. **Resolves the "OpenOCD for flashing TBD" gap** — drag-and-drop works for now; STM32CubeProgrammer can be installed later for debugging (breakpoints, register inspection). Next: MCU-to-modem UART link (verify SIM7600 UART voltage, wire Nucleo→HAT, add UART node to app.overlay, write AT command test). | Done |
| 2026-07-13 | **SIM7600 UART voltage verified from datasheet.** Hardware Design Manual V1.03 §3.3.1: "The module UART is 1.8V voltage interface." Table 30: digital pins (UART, PCM, GPIO) absolute max 2.1V — 3.3V would damage the raw module. Table 32: VIH 1.17–2.1V, VOH 1.35–1.8V. SIM7600 UART pins: TXD=71, RXD=68, RTS=66, CTS=67, RI=69, DCD=70, DTR=72. **Waveshare HAT has onboard voltage translator** — converts SIM7600 1.8V UART to 3.3V (configurable to 5V via VCCIO jumper, default 3.3V for RPi). No external level shifter needed for prototyping. Final PCB will need TXB0108 between STM32H743 and SIM7600A-H. | Done |
| 2026-07-13 | **MCU-TO-MODEM UART LINK ESTABLISHED: AT commands working from STM32 to SIM7600.** Wired Nucleo LPUART1 (PB6 TX / PB7 RX, Arduino D1/D0) to Waveshare HAT breakout pins (TXD/RXD/GND). HAT powered via own USB (modem draws 2A+ peaks — Nucleo USB can't supply that for calls). UART selection jumper on HAT stays on **B** (routes SIM7600 UART to header/breakout pins for external MCU control — not C, which routes to CP210x USB bridge). **This also explains the Phase 2 CP210x mystery**: jumper was on B, routing UART to header pins, not to the CP210x — the CP210x was never connected to the SIM7600's UART. Firmware: `main.c` uses Zephyr polling UART API (`uart_poll_in`/`uart_poll_out`) on LPUART1 (already in board DTS, no overlay changes needed). First two attempts had character loss: (1) `k_msleep(1)` between polls was too slow — at 115200 baud chars arrive every 87µs, 1ms sleep caused RX buffer overrun (lost ~11/12 chars in bursts); (2) 100ms gap timeout too short + no buffer drain between commands. Fixed by: `continue` after each char (poll immediately for next), `k_busy_wait(50)` instead of `k_msleep(1)`, 500ms gap timeout, `modem_drain()` before each command, 200ms delay between commands. **All four test commands returned complete responses**: AT→OK, AT+CSQ→+CSQ: 21,99, AT+CEREG?→+CEREG: 0,1 (registered home LTE), AT+CGMM→SIMCOM_SIM7600NA-H. Firmware size: 61,300 B flash (2.92%), 15,620 B RAM (2.98%). **This is the entire MCU-to-modem control path** — every phone function (dial, answer, hang up, SMS, signal) goes through this UART. Next: upgrade to interrupt-driven UART (polling misses async URCs like incoming-call notifications), then build AT command parser + call state machine. | Done |
| 2026-07-13 | **Upgraded modem UART to interrupt-driven with ring buffer.** Replaced polling (`uart_poll_in`/`k_busy_wait`) with interrupt-driven API (`uart_irq_callback_user_data_set` + `uart_irq_rx_enable` + `uart_fifo_read`). Architecture: ISR fires on each UART RX interrupt, reads hardware FIFO into a 1KB `RING_BUF_DECLARE` ring buffer, returns immediately (no sleeping/logging in ISR — interrupt context rules). Application reads from ring buffer with `k_msleep(1)` polling — can sleep peacefully because ISR catches every char in background. Added `CONFIG_UART_INTERRUPT_DRIVEN=y` to prj.conf. **This is the production-ready architecture** — can receive async URCs (incoming-call `RING`, SMS `+CMTI`, network status changes) even while app is busy with UI/keypad/other work. All four AT test commands return complete responses (AT→OK, AT+CSQ→+CSQ: 20,99, AT+CEREG?→+CEREG: 0,1, AT+CGMM→SIMCOM_SIM7600NA-H). Firmware size: 61,456 B flash (2.93%), 16,708 B RAM (3.19%). Next: AT command parser (separate echo from response, parse structured data, handle OK/ERROR), then place a call from firmware. | Done |
| 2026-07-13 | **MVP ACHIEVED: MCU places and receives VoLTE calls.** Firmware sends `ATD6078821755;` → `VOICE CALL: BEGIN` (call connected in ~6s) → `VOICE CALL: END: 000010` (other party hung up). Then enters incoming-call monitor loop — detected `RING` URC → auto-answered with `ATA` → `VOICE CALL: BEGIN` → `VOICE CALL: END: 000007`. **Both outgoing and incoming calls work from MCU firmware with zero PC involvement.** Audio goes through HAT's NAU8810 codec (headphone jack). URC monitor loop uses `modem_readline()` to process one line at a time from the ring buffer — the interrupt-driven architecture catches async `RING` notifications while the app sleeps in the read loop. Hit one bug: stack overflow (MPU fault) because `modem_at()` allocates 512-byte local buffer + `modem_readline()` 256 bytes, exceeding Zephyr's default 1024-byte main stack. Fixed with `CONFIG_MAIN_STACK_SIZE=4096`. Firmware size: 62,456 B flash (2.98%), 16,708 B RAM (3.19%). **This is the MVP success criterion from `docs/problem-definition.md`: "Successfully place and receive a voice call from the custom-built device to another phone, with intelligible audio, on a US LTE network."** The device is the Nucleo+HAT (not yet a custom PCB), but the firmware architecture is proven. Next: display (ST7789V) and keypad to make it a standalone device, then PCB design. | Done |
| 2026-07-13 | **Display bring-up: ST7789V 2.0" 240×320 TFT working on SPI1.** Waveshare 2" LCD module wired to Nucleo morpho headers: DIN→PB5 (MOSI), CLK→PA5 (SCK), CS→PD14, DS(DC)→PD15, RST→PF3, BL→PG12, VCC→3.3V, GND→GND. Devicetree overlay (`app.overlay`) configures `zephyr,mipi-dbi-spi` wrapper + `sitronix,st7789v` panel node with Waveshare panel params (from Zephyr's `st7789v_waveshare_240x240.overlay` shield). Backlight via `regulator-fixed` on D7 (PG12). Added `CONFIG_DISPLAY=y` to prj.conf. Firmware uses `display_write()` API — fills screen row-by-row (240px × 2 bytes = 480B row buffer, static) to show call status: blue=idle, green=connected/modem OK, yellow=calling. **Display + calls work simultaneously** — outgoing call, incoming call auto-answer, and color status display all confirmed. One cosmetic issue: shell UART RX ring buffer overflow warnings during display fills (320 SPI writes block main thread briefly, shell log backend can't drain). Not a functional problem — shell is debug-only. Can fix later by disabling shell or moving display to separate thread. Firmware size: 70,840 B flash (3.38%), 20,420 B RAM (3.89%). Next: LVGL for text UI (phone number, signal bars, call timer), then keypad. | Done |
| 2026-07-13 | **LVGL text UI + B1 button call control + AT+CHUP hangup fix.** Three upgrades in one session: (1) **LVGL integration** — replaced raw `display_write` color fills with LVGL labels (Montserrat 28 font, white status / green number / gray info on black background). Added `CONFIG_LVGL=y`, `CONFIG_LV_Z_BITS_PER_PIXEL=16`, `CONFIG_LV_COLOR_16_SWAP=y`, `CONFIG_LV_USE_LABEL=y`, `CONFIG_LV_FONT_MONTSERRAT_28=y` to prj.conf. `lv_timer_handler()` called in main loop for rendering. (2) **B1 button state machine** — no more auto-call on boot or auto-answer. The Nucleo's built-in user button (PC13, `sw0` in board DTS) drives a 4-state machine: IDLE→press to call (`ATD<number>;`), CALLING→press to cancel (`AT+CHUP`), RINGING→press to answer (`ATA`), CONNECTED→press to hang up (`AT+CHUP`). Button uses GPIO interrupt (ISR sets flag, main loop debounces with 50ms + re-read). (3) **AT+CHUP hangup fix** — `ATH` returned `OK` but didn't actually end voice calls (it's for data calls). `AT+CHUP` (Call Hang Up) sends the SIP BYE and tears down the VoLTE session properly. All confirmed working: place call, hang up, receive call, answer, hang up — all from B1 button with LVGL status text on display. Firmware size: 328,464 B flash (15.66% — LVGL is large but we have 2MB), 52,356 B RAM (9.99%). Next: keypad for dialing arbitrary numbers (blocked on soldering), then PCB design. | Done |
| 2026-07-13 | **Post-MVP roadmap planning.** Audited MVP feature table: make/receive calls done, signal indicator doable now (`AT+CSQ`), battery indicator blocked on PCB (MAX17048 fuel gauge needs battery power path — HAT's regulator prevents validation). Battery hardware deferred to PCB. Planned next session (~90 min): 4×4 matrix keypad + arbitrary calling + signal indicator = all doable-now MVP items complete. After that: PCB schematic kickoff (KiCad), with firmware hardening + daily-driver features during fab wait. | Planned |
| 2026-07-18 | **Keypad integration: gpio-kbd-matrix driver + dial string UI.** Added `gpio-kbd-matrix` devicetree node to `app.overlay` (4 rows on D3–D6, 4 cols on A1–A3 + D2, interrupt idle mode, 10/20ms debounce). Enabled `CONFIG_INPUT=y` in `prj.conf`. Rewrote main.c: input callback (`INPUT_CALLBACK_DEFINE`) captures `(col, row)` from `INPUT_ABS_X/Y` + `INPUT_BTN_TOUCH` events, queues key char via `K_MSGQ_DEFINE(key_msgq, ...)` to the main loop. Keymap maps (row,col) → character per Adafruit 4×4 layout. `handle_key()` replaces `handle_button()`: digits append to `dial_string`, A=Call (`ATD<string>;`), B=End/Cancel/Reject (`AT+CHUP`), C=Backspace, D=Menu (no-op). B1 button removed (redundant). Thread safety: callback only does `k_msgq_put` (thread-safe); all LVGL/modem access stays in main loop. Build clean: 333,956 B flash (15.92%), 56,980 B RAM (10.87%). Driver verified bound via linker map. **Not yet tested on hardware** — keypad wiring + functional test is next. | Done (code) / Pending (hardware test) |
| 2026-07-18 | **Keypad EXTI conflict fixed + hardware test passed.** First flash failed: `<err> input_gpio_kbd_matrix: Pin 0 interrupt configuration failed: -16` (EBUSY). Root cause: STM32 EXTI line conflict — row 0 is PE13 (EXTI13), but board DTS defines B1 user button on PC13 (also EXTI13). STM32 EXTI lines 0–15 are port-shared (one port per line); `gpio-keys` claimed PC13/EXTI13 first, blocking PE13. Fix: `&user_button { status = "disabled"; }` in `app.overlay` — B1 was already removed from app code (keypad A/B replaced it), so disabling the board node frees EXTI13. Build verified `status = "disabled"` in compiled DTS. After reflash: clean boot, no errors. **Functional test passed**: typed `6077936142` (cleared a `123` typo with C/backspace), A → `ATD6077936142;` → `VOICE CALL: BEGIN` (~10s connect) → B → `AT+CHUP` → `VOICE CALL: END: 000044`. **Real VoLTE call to an arbitrary user-typed number, no PC involvement.** Standalone-dialer milestone reached. Firmware: 333,944 B flash (15.92%), 56,916 B RAM (10.86%). | Done |
| 2026-07-18 | **UI redesign: TE-inspired industrial design with JetBrains Mono.** Replaced the 80s sci-fi HUD aesthetic (amber on black, amber borders) with a Teenage Engineering-inspired industrial design: layered grey palette (`#1A1A1A` base, `#262626` panels, `#3A3A3A` borders), amber reserved for content only. Status bar: grey panel with signal (left) / time (center) / battery (right) — carrier label removed. Softkey bar: grey panel with grey border. Scanlines retained for CRT phosphor depth. **Fonts**: Replaced Unscii 8/16 with JetBrains Mono (converted from TTF via `lv_font_conv` npm tool — 3 sizes: 10px status bar, 14px content/softkeys/menu, 22px bold dialer number). Font files in `firmware/src/fonts/`, added to CMakeLists.txt. Node.js installed for font conversion workflow. Zone heights adjusted (22px status, 26px softkey). "IDLE" state label removed from dialer (cleaner). Build: 303,536 B flash (14.47%), 56,916 B RAM (10.86%). See `ui-mockup/index.html` for the reference mockup. | Done |
| 2026-07-18 | **Full UI port: all 10 mockup screens migrated to firmware with mock data.** Migrated every screen from the HTML mockup to LVGL firmware: (1) **Dialer** — digits + blinking cursor, A=call, C=backspace, D=menu; (2) **Menu** — CONTACTS/MESSAGES/SETTINGS, 2/8 scroll, A select, B back; (3) **Contacts list** — 5 mock contacts, 2/8 scroll, A open, B back; (4) **Contact detail** — name + number + 4 actions (CALL/TEXT/EDIT/DELETE), CALL places real `ATD` call; (5) **Messages list** — 3 mock conversations with last-message preview; (6) **Conversation** — message thread (`<` incoming, `>` outgoing), C=compose, B=back; (7) **SMS Compose** — mock text entry with blinking cursor, A=send (logs only, no real `AT+CMGS`); (8) **In-call** — call timer (MM:SS) counting up from connect; (9) **Incoming** — INCOMING label + number, A=answer, B=reject; (10) **Calling** — CALLING... label + number, B=cancel. Architecture: unified `ui_render()` function replaces per-screen `ui_show()`/`ui_show_menu()` — hides all content labels then shows+populates only the ones needed. 5 flexible content labels (title/hero/list/timer/cursor) cover all screens. Blinking cursor via LVGL `lv_anim_t` (500ms cycle). Call timer via `k_uptime_get()` in main loop. All contacts/messages data are hardcoded arrays (real persistence + real SMS are future work). Build: 306,136 B flash (14.60%), 56,980 B RAM (10.87%). | Done |
